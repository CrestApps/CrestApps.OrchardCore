@using CrestApps.OrchardCore.AI.Memory.ViewModels

@model ChatInteractionMemorySettingsViewModel

@if (!Model.HasIndexProfile)
{
    <div id="chatInteractionMemoryIndexWarning" class="alert alert-warning @(Model.EnableUserMemory ? string.Empty : "d-none")" role="alert">
        <i class="fa-solid fa-triangle-exclamation me-2"></i>
        <strong>@T["Index not configured"]</strong>
        <p class="mb-0">
            @T["No AI memory index is configured. Go to Settings > Artificial Intelligence > Memory and choose an index profile before enabling user memory for chat interactions."]
        </p>
    </div>
}

<div class="mb-3">
    <div class="form-check">
        <input type="checkbox" class="form-check-input" asp-for="EnableUserMemory" />
        <label class="form-check-label" asp-for="EnableUserMemory">@T["Enable user memory"]</label>
        <span class="hint">@T["When enabled, authenticated users can build private AI memory across chat interactions. Anonymous users never get memory access."]</span>
    </div>
</div>

@if (!Model.HasIndexProfile)
{
    <script at="Foot">
        (function () {
            const toggle = document.getElementById('@Html.IdFor(m => m.EnableUserMemory)');
            const warning = document.getElementById('chatInteractionMemoryIndexWarning');
            if (!toggle || !warning) return;

            const syncWarning = function () {
                warning.classList.toggle('d-none', !toggle.checked);
            };

            toggle.addEventListener('change', syncWarning);
            syncWarning();
        })();
    </script>
}
