[default]
exten => _X.,1,NoOp(CrestApps Asterisk local test call to ${EXTEN})
 same => n,Answer()
 same => n,Wait(1)
 same => n,PlayTones(440/250,0/100,523/250,0/100,659/500)
 same => n,Wait(2)
 same => n,StopPlayTones()
 same => n,Echo()
 same => n,Hangup()

exten => _+X.,1,NoOp(CrestApps Asterisk local test call to ${EXTEN})
 same => n,Answer()
 same => n,Wait(1)
 same => n,PlayTones(440/250,0/100,523/250,0/100,659/500)
 same => n,Wait(2)
 same => n,StopPlayTones()
 same => n,Echo()
 same => n,Hangup()

[crestapps-simulation]
exten => 2001,1,NoOp(CrestApps synthetic simulation party A)
 same => n,Answer()
 same => n,PlayTones(440/250,0/750)
 same => n,Wait(300)
 same => n,StopPlayTones()
 same => n,Hangup()

exten => 2002,1,NoOp(CrestApps synthetic simulation party B)
 same => n,Answer()
 same => n,PlayTones(659/250,0/1250)
 same => n,Wait(300)
 same => n,StopPlayTones()
 same => n,Hangup()
