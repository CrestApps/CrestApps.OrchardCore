---
sidebar_label: Unreleased
sidebar_position: 1
title: Unreleased
description: Changes that are available in the current development branch and not yet included in a tagged release.
---

# Unreleased

## Documentation

- Added a new **Framework** documentation section that documents each framework feature independently — covering core services, AI core, orchestration, chat, document processing, templates, tools, response handlers, context builders, SignalR, data storage, providers, data sources, MCP, and A2A.
- Each framework doc follows a consistent structure: quick start extension method, problem & solution, core interfaces, configuration, and implementation examples.
- Added an [MVC Example walkthrough](../framework/mvc-example.md) that documents all 13 sections of the `CrestApps.Mvc.Web` startup configuration.
- Updated the Introduction and Getting Started pages to present both Framework (standalone .NET) and Orchard Core paths.
- Updated the AI Suite index to reference framework docs for core concepts (orchestration, context builders, tools, etc.).
- Created `AddCrestAppsMcpClient()` and `AddCrestAppsMcpServer()` DI extensions that bundle previously scattered MCP service registrations into two cohesive calls.
- Cleaned up `CrestApps.Mvc.Web/Program.cs` with comprehensive section documentation aligned with the new framework docs.
- Documented why `AppDataConfigurationFileService` writes to `App_Data/appsettings.json`: it keeps admin-managed settings in the same reloadable configuration file so changes persist and flow back through ASP.NET Core configuration reload.
- Updated the framework docs to reflect the new `CrestApps.Infrastructure` layer, the `CrestApps.AI.Copilot` package name, and the renamed `CrestApps.Elasticsearch` / `CrestApps.Azure.AISearch` search-provider packages.
- Updated the framework docs again to reflect the new `CrestApps.Infrastructure.Abstractions` layer and the MVC sample's area-based feature layout.

## AI

- Replaced the Orchard Core AI site-setting option overlays for General AI, Data Sources, Documents, and Memory with `IConfigureOptions<T>` registrations, added tenant reload warnings plus shell-release requests on the related settings editors, removed the unused Azure AI Search / Elasticsearch framework service registrations from the Orchard Core AI data-source provider modules, and replaced the remaining Orchard Core provider integration points with Orchard-backed adapters that resolve index profiles, provider index managers, provider document managers, and search clients from Orchard Core itself.
- Consolidated Orchard Core document, data-source, and memory index-profile embedding settings onto the shared `DataSourceIndexProfileMetadata` deployment selector, removed the dedicated chat-document and memory metadata types, and added an Orchard Core indexing migration that upgrades older tenant index profiles to the shared deployment-based metadata.
- Restored shared framework-level A2A client support through a new `CrestApps.AI.A2A` project so Orchard Core and `CrestApps.Mvc.Web` now consume the same A2A connection models, authentication services, agent-card cache, tool registry provider, and discovery functions.
- Added MVC admin CRUD management for A2A host connections plus A2A capability selectors on AI profiles, AI profile templates, and chat interactions so stored connections flow into orchestration and remote A2A agents can be selected without duplicating MVC-only A2A service layers.
- Moved `SearchDocumentsTool`, `DataSourceSearchTool`, the default plain-text document reader, and the default AI document processor into the shared framework so Orchard Core and the MVC sample use the same host-agnostic document/data-source search pipeline.
- Added MVC JSON-backed settings for the default **AI Documents** index and default retrieval behavior, then updated MVC document uploads to process, embed, and index document chunks into that configured search index instead of only storing raw uploads.
- Moved the OpenXml and PDF ingestion readers into the shared framework so MVC and Orchard Core use the same document extraction path for embeddable uploads.
- Added MVC admin management for MCP connections, prompts, and resources, backed by shared framework MCP client/server services instead of MVC-specific protocol implementations.
- Added shared framework MCP FTP/SFTP resource metadata, handlers, and SSE/OAuth client plumbing so Orchard Core and MVC can expose the same MCP protocol capabilities with host-specific UI only.
- Updated the MVC admin experience with the requested dashboard section split, capability-oriented profile selection surfaces, persisted MCP server settings, and the A2A authentication form fix that reveals the correct fields per authentication mode.
- Replaced the MVC `ChatApiController` with three minimal API endpoints and aligned the sample app with the Orchard Core endpoint-style bootstrapping pattern.
- Moved chat interaction and chat session document upload/remove endpoints into the shared framework chat layer, added host-pluggable authorization and event hooks, and updated Orchard Core plus MVC to reuse the same HTTP contract.
- Updated the MVC chat UIs so chat interactions upload/remove knowledge documents immediately via AJAX, AI chat sessions expose session document uploads only when the profile enables them, AI Template list buttons match the AI Connections styling, and the MCP Hosts list no longer shows the redundant Details column.
- Aligned `CrestApps.Mvc.Web` Copilot settings and Copilot orchestrator readiness checks with Orchard Core so the sample app now respects the `NotConfigured` state, hides the wrong auth-specific UI when Copilot is disabled, and only marks Copilot as ready when the same OAuth/BYOK requirements are satisfied.
- Finished the remaining MVC Copilot parity gaps by wiring Orchard-style Copilot warnings and GitHub sign-in/status UI into AI profile templates, copying template Copilot metadata into created profiles, renaming the AI Deployment editor to use `Technical name` and `Model name`, and persisting chat interaction deletes so confirmed removals actually disappear.
- Fixed the MVC Copilot orchestrator panel so it correctly shows the "not configured" warning when a user switches to the Copilot orchestrator, regardless of the current authentication type. Previously the panel toggle JavaScript was only rendered for GitHubOAuth mode, so the warning never appeared when Copilot was unconfigured.
- Fixed the MVC Chat Interaction chat sidebar orchestrator selector so the Copilot panel toggle JavaScript can find it (added the required `name` attribute).
- Simplified MVC Chat Interaction creation flow: clicking "New Chat" now auto-creates a blank interaction and immediately opens the full chat view with settings sidebar and chat window side by side, eliminating the separate two-step create-then-chat workflow.
- Registered `AddLocalization()` in the MVC startup so `IStringLocalizerFactory` is available to shared framework endpoints (fixes a `NullReferenceException` in the document upload endpoints).
- Added a complete **Articles** admin feature to the MVC sample app demonstrating custom knowledge-base data sources: CRUD management with Markdown descriptions, YesSql-backed catalog storage with `ArticleIndex`, automatic search indexing via `ArticleIndexingHandler` (Elasticsearch and Azure AI Search), configurable pagination settings (default 25 items per page) shared across all admin lists, a reusable `_Pager` partial, six seed articles covering AI and search indexing topics, and a registry-backed index-profile source system so `"Articles"` appears as a first-class index profile type for both Elasticsearch and Azure AI Search.
- Split the framework search-provider integrations into dedicated `CrestApps.Elasticsearch` and `CrestApps.Azure.AISearch` projects so provider-specific dependencies and services (client setup, index managers, document managers, vector search, OData translation, and source readers/content managers) now live in separate reusable libraries. The older `CrestApps.AI.DataSources.*` framework projects were removed so the provider libraries are the canonical indexing backends for data sources, documents, and memory.
- Added a lower `CrestApps.Infrastructure` framework layer for host-agnostic helpers and constants, moved prompting into the core framework folder, and renamed the Copilot integration project to `CrestApps.AI.Copilot` so the framework structure reads as infrastructure first and AI on top.
- Added `CrestApps.Infrastructure.Abstractions` and moved search, indexing, document-chunk, and data-source contracts out of `CrestApps.AI.Abstractions` so `CrestApps.Azure.AISearch` and `CrestApps.Elasticsearch` now depend on infrastructure-level contracts instead of AI-level abstractions.
- Reorganized the remaining `CrestApps.AI.Abstractions` project into feature folders and split the MVC sample's former catch-all admin surface into dedicated `AI`, `AIChat`, `ChatInteractions`, `A2A`, `Mcp`, `DataSources`, and `Indexing` areas while keeping shared settings and articles under `Admin`.
- Consolidated MVC admin-managed settings into a single `App_Data/appsettings.json` override file layered on top of `appsettings.json` and `appsettings.Development.json`. Settings now live only as nested sections in those three files; the legacy per-setting JSON files are no longer part of the configuration flow. Search providers now bind from `CrestApps:Elasticsearch` and `CrestApps:AzureAISearch`, matching the existing local override file structure.
- Removed MVC-only backward-compatibility shims for the old `AddElasticsearchIndexingSource()` and `AddAzureAISearchIndexingSource()` registration names so the sample app exposes only the clean `AddElasticsearchDataSource()` and `AddAzureAISearchDataSource()` API surface.
- Refined the MVC index-profile registration surface so built-in provider/type combinations now use explicit extensions such as `AddElasticsearchAIDocumentSource()`, `AddElasticsearchAIDataSource()`, and `AddAzureAISearchAIMemorySource()`, which register both the source descriptor and the matching `IIndexProfileHandler`.
- Updated the MVC shared layout to always show server-side `ModelState` errors and redirected `ErrorMessage` alerts, and relaxed MVC index-profile deletion so admins can still remove a local profile when the remote Elasticsearch or Azure AI Search index was already deleted manually or the stored profile can no longer resolve a remote index name.
- Relaxed MVC index-profile deletion further so admins can still remove a local profile even when the original provider registration is no longer present in the app.
- Fixed the shared `CatalogManager<T>.DeleteAsync()` path to commit catalog deletions through `SaveChangesAsync()`, which also fixes MVC index-profile deletes persisting to the YesSql store.
- Updated the MVC chat UIs so Chat Interactions no longer shows a redundant attachment button next to the message box, the widget now exposes direct session-document uploads whenever the selected AI profile enables them, and MVC index profiles now require embeddings for AI Documents / AI Memory / Data Source types, hide the selector for Articles, compose remote index names from provider `IndexPrefix` + index name, provision the remote index before saving the local profile, reject creates when the remote index already exists, lock index/provider/type/embedding after creation, and delete the remote search index when the profile is removed.
- Fixed shared AI memory recall in `CrestApps.Mvc.Web` by resolving memory-search dependencies inside a fresh scope per request and by registering provider-specific `IMemoryVectorSearchService` adapters from the shared Azure AI Search and Elasticsearch framework packages, so preemptive memory retrieval and memory search tools now work outside Orchard Core too.
- Regrouped the MVC AI settings UI so the memory toggles stay under **Memory** and the caching, diagnostics, and pagination controls now sit under **Orchestration**.
- Moved Orchard Core AI resolution detection into the shared **Data Processing & Metrics** analytics editor for chat AI Profiles and profile-sourced AI Templates, added an Orchard **Chat Conversion Goals** report with profile/date filters and dynamic goal columns, and aligned the MVC sample so chat sessions now persist initial assistant prompts, the admin widget restores its open state/session across navigation, attached knowledge documents stand out visually in profile/template editors, and a sample `sendEmail` AI tool logs outbound email requests.
- Fixed the remaining MVC AI Chat parity gaps for session metrics and chat history UX: the main AI Chat page and admin widget now honor each profile's **Enable session metrics** setting so hover thumbs up/down buttons appear alongside the copy action, the widget uses the same compact lower-right hover toolbar and light message separators as the other chat UIs, and the recent sessions list now sorts newest-first by creation time.
- Restored the MVC **Admin widget** site settings so administrators can explicitly choose which AI profile powers the floating admin chat, leave it on **No profile (Disable)** to hide the widget entirely, and optionally override the widget accent color. Without an override, the widget now uses the admin theme secondary color instead of the old hard-coded green.
- Fixed the MVC admin chat widget boot flow so it now starts a real session immediately for the configured profile. Profiles with an initial prompt now show that initial assistant message first, matching the main AI Chat page, while the widget color default is now explicitly persisted as the secondary-theme value `#6c757d`.
- Added a new **AI Usage Analytics** report to both Orchard Core and `CrestApps.Mvc.Web`, backed by shared usage tracking at the `IChatClient` factory layer so direct `IAIClientFactory` calls, orchestrator sub-requests, and tool-driven follow-up calls all record provider/client/model usage per authenticated user or anonymous visitor across chat sessions and chat interactions.
- Added a global **Enable AI usage tracking** AI setting for Orchard Core and `CrestApps.Mvc.Web`, disabled by default so provider-call tracking stays opt-in. The **AI Usage Analytics** report now shows a warning when usage tracking is off, while per-profile **Enable Session Metrics** continues to control chat-session analytics independently.
- Updated `CrestApps.Mvc.Web` AI settings and chat-profile editors to use Orchard-style chat mode configuration, replaced the default text-to-speech voice textbox with a deployment-driven voice picker, fixed the MVC settings DI path so chat-interaction mode loads from the persisted `ChatInteraction` settings section, and wired the MVC AI Chat page, Chat Interactions page, and admin widget to honor text/audio/conversation modes end to end.
- Removed the duplicate Orchard Core `DefaultSpeechVoiceResolver` so speech voice resolution now lives only in the shared framework, and moved `DataSourceSearchTool` registration into `AddDataSourceRagServices()` so Orchard Core and `CrestApps.Mvc.Web` pick up the same data-source search capability without host-specific wiring.
- Replaced the MVC sample app's ad-hoc `/a2a` authentication middleware with an Orchard-style A2A API-key authentication scheme plus authorization policy, and taught both A2A sample clients to send `A2A:ApiKey` as a bearer token when configured so the samples keep working against protected hosts.
- Fixed configuration-backed standalone AI deployments so raw `ApiKey` values from `appsettings` are consumed as-is instead of being data-protected while loading the deployment catalog, which restores Azure Speech and other contained-connection deployments that authenticate directly from configuration.
- Fixed MVC speech input after the Azure Speech auth changes by preferring the browser's full locale in chat UIs and normalizing neutral cultures such as `en` to specific locales such as `en-US` in the shared Azure Speech client.
- Aligned MVC conversation-mode UX so AI Chat and Chat Interactions use the same conversation button treatment, restored the inactive AI Chat button styling after ending conversation mode, and auto-dismiss the conversation-ended notice with improved widget spacing.
- Reduced chat-conversation speech latency by sending MVC and shared chat audio chunks more frequently, and updated MVC Chat Interactions to hide the mic/send buttons while conversation mode is active so the control state matches the live voice experience.
- Fixed MVC AI Profile template discovery so the create-profile template picker now merges embedded `Templates/Profiles/*.md` resources from the shared `CrestApps.AI` assembly, and the core AI project now embeds the whole profile-template folder instead of a single hard-coded file.
- Bridged Orchard Core's native `IIndexProfileStore` into the shared `ISearchIndexProfileStore` contract so shared document/data-source RAG handlers and search tools can resolve Orchard tenant index profiles without DI failures, and regrouped Orchard Core AI profile editors so **Apply Templates** uses a dedicated card, **Knowledge** stays as the knowledge tab, and chat-specific settings now live together under **Actions**.
- Hardened Orchard Core chat document upload/remove handling so the Orchard-only document indexing event handler no longer hard-fails when Orchard indexing services are unavailable for the current tenant. Attachments now save and delete normally while remote index synchronization is skipped until indexing is enabled again.
- Fixed Orchard Core chat document upload/remove routing by assigning route names to the shared upload/remove endpoints before the Orchard views generate URLs, which stops document actions from posting back to the current admin chat page. The shared chat UI and the Orchard chat-interaction document editor now also collapse unexpected HTML error responses into short readable messages instead of surfacing raw page markup.
- Fixed the MVC **Chat Analytics** feedback section to mirror the Orchard Core presentation with positive/negative count tiles plus the same satisfaction progress bar.
- Corrected chat-session analytics accumulation so token totals are recorded from completion usage while average response latency is tracked separately from visible assistant responses instead of being mixed into one counter.
- Refined the MVC index-profile implementation to align even more closely with Orchard Core indexing: the sample now resolves provider-specific work through keyed `ISearchIndexManager`, those methods accept `IIndexProfileInfo` instead of raw index-name strings, and shared `IIndexProfileHandler` implementations now validate embedding requirements and provide schema fields so the temporary `ISearchIndexProfileManager` layer is no longer needed.
- Cleaned up MVC dropdown handling so controller-populated `SelectListItem` collections now contain only real values, while Razor views render the empty placeholder option directly with plain labels such as `Select provider`, `Use default orchestrator`, and `None` instead of dash-wrapped placeholders.
- Fixed MVC Chat Interactions data-source RAG by persisting `DataSourceMetadata` and `AIDataSourceRagMetadata` through the shared chat settings pipeline, seeding default RAG metadata when a chat interaction is created with a data source, and moving both the chat-settings handler and data-source preemptive RAG handler registrations into the shared framework/provider extensions so knowledge-base context is injected during chat requests without MVC-only wiring.
- Fixed MVC Chat Interaction settings persistence so the shared save path now applies `DocumentTopN` and `AgentNames`, stores prompt-template selections in `PromptTemplateMetadata`, and reloads those saved prompt templates back into the chat editor after refresh.
- Fixed MVC Chat Interaction data-source reload so the chat editor now rehydrates the saved selection from `DataSourceMetadata` and the saved RAG values from `AIDataSourceRagMetadata` when the page is reopened.
- Expanded the MVC Knowledge tabs so Chat Interactions and AI Profiles both expose the `AIDataSourceRagMetadata` fields (`Strictness`, `TopNDocuments`, `IsInScope`, and `Filter`), the Chat Interaction knowledge tab now separates data source, RAG, and document-upload sections visually, and chat document uploads now show visible progress, per-file status, and success/error feedback instead of only logging failures to the console.
- Aligned the MVC AI Profile editor with the Orchard Core storage model so `AIProfileSettings`, data extraction, post-session processing, and profile memory use the `Settings` store APIs while profile documents and session documents continue to use metadata in `Properties`. The MVC profile mapper now also uses `Alter<>` consistently for metadata-backed writes, including prompt templates, analytics, documents, session documents, and Copilot metadata, and fixes the saved RAG filter/top-N mapping.
- Hardened the MVC sample's view models so every `SelectListItem` option collection now uses `IEnumerable<SelectListItem>` with `[BindNever]`, and aligned chat-interaction creation plus live settings saves on `Alter<>` so prompt templates, Copilot metadata, and knowledge-base metadata persist consistently.
- Fixed flattened `JsonExtensionData` reloads for AI profiles and other extensible entities by teaching the shared `As<T>()` / `TryGet<T>()` helpers to deserialize dictionary-backed property values in addition to `JsonElement` and `JsonNode`, which restores saved data-source selections after MVC profile edits are reloaded from storage.
- Fixed AI profile JSON serialization so `Settings` stays as the nested settings bag while extensible metadata remains flattened in `Properties`, avoiding the `JsonExtensionData` conflict that occurs when `System.Text.Json` sees more than one extension-data member on the same profile model.
- Finished the remaining profile-side storage cleanup so shared completion-context building, agent profile inspection/listing tools, and the chat-session close background task now read `...Metadata` from `Properties` and `...Settings` from the `Settings` bag consistently with the Orchard Core implementation.
- Updated the MVC Chat Interaction editor so model-tuning parameters stack one-per-row until extra-large layouts, each parameter label shows the same hover help affordance used in Orchard Core, and duplicate chat document uploads are rejected with user-visible feedback instead of being processed twice.
- Added shared framework support for defining provider connections in `CrestApps:AI:Connections` as an array of connection records with required `Name` and `ClientName` properties, merged those appsettings-defined connections with UI-managed MVC connections without overwriting config entries, updated the MVC AI Deployment editor to populate its connection dropdown from the merged runtime options, and aligned Orchard Core's UI-managed connection projection to reuse the same shared merge path.
- Moved configuration-defined AI deployment merging into the shared framework through `ConfigurationAIDeploymentCatalog`, so MVC and Orchard Core now both read appsettings deployments together with UI-managed deployments from one central path. The shared catalog supports flat `CrestApps:AI:Deployments` arrays, provider-grouped deployment objects, and connection-scoped `Deployments` arrays inside provider connection settings while leaving writes routed to the persisted deployment store only.
- Registered `AzureSpeech` in the shared Azure OpenAI framework extension as a deployment provider with contained-connection support, so MVC hosts that call `AddAzureOpenAIProvider()` now accept `CrestApps:AI:Deployments` speech entries from appsettings and surface them in the speech deployment selectors together with UI-managed deployments.
- Updated the MVC AI Connections and AI Deployments admin pages so system-configuration entries render in separate read-only cards below the user-defined records. The UI still blocks edit and delete actions for those configuration-defined records while keeping persisted UI-managed entries editable.
- Tightened document-grounded prompting for AI Profiles so profile knowledge-base documents now tell the model to search document tools first, and updated `SearchDocumentsTool` plus chat-interaction defaults so strict document mode no longer nudges the model back toward general knowledge and new chat interactions start with **Restrict answers to retrieved data only** turned off by default.
- Moved document preemptive RAG into the shared framework registration path so both `CrestApps.Mvc.Web` and Orchard Core now inject retrieved document chunks before generation when preemptive RAG is enabled. This fixes MVC AI Profiles that previously advertised document tools but did not actually preload profile document context into the prompt.
- Moved the remaining Orchard-only AI memory preemptive-RAG and orchestration handlers into the shared framework, introduced `AddAIMemoryServices()` so MVC and Orchard Core share the same memory tools/safety/runtime behavior, and switched the MCP client extension to register the shared profile-context completion handler instead of leaving that logic in an Orchard-only module.
- Replaced the remaining shared AI settings-provider wrapper interfaces with `IOptions<>`-backed runtime options. `GeneralAIOptions`, `ChatInteractionMemoryOptions`, `InteractionDocumentOptions`, and `AIDataSourceOptions` now drive the shared framework behavior, Orchard Core composes tenant site settings onto those scoped options, MVC binds them from app-data configuration, and `IAIMemorySearchService` now uses a single `IEnumerable<string>` search signature.
- Removed the temporary `SaveUserMemoryTool` server-side description rewrite so memory descriptions now stay exactly as the model provided them, strengthened the shared memory prompt/schema guidance to keep `description` category-oriented and `content` value-oriented, and added shared AI chat orchestration instructions that feed already extracted session fields back into AI Profile conversations so scripted chat flows stop re-asking for values that were already collected earlier in the same session.
- Fixed MVC AI chat session ordering and initial-prompt parity by timestamping normal session prompts and storing AI Profile initial prompts as regular assistant turns instead of generated-only prompts. This keeps reload order stable and ensures follow-up replies are grounded on the visible initial assistant message.
- Moved the shared chat-session runtime deeper into the framework: `DataExtractionService`, `PostSessionProcessingService`, the post-close processor, and the default extraction/post-close handlers now live in `CrestApps.AI.Chat`, Orchard Core records only its workflow/analytics host-specific side effects on top of those shared results, MVC now uses the same post-close processor for inactivity retries, and document upload flows resolve embedding generators through `IAIClientFactory` instead of asking `IAIDocumentProcessingService` to create them.
- Removed the fallback `NoOpAIMemorySearchService` and moved the real `AIMemorySearchService` into the shared framework. AI Memory now reads `AIMemoryOptions` through `IOptions<>`, Orchard Core overlays those options from tenant `AIMemorySettings`, MVC binds them from app-data-backed site settings, and the remaining framework-side clock usage now relies on `TimeProvider`.
- Added Orchard-style citation handling to `CrestApps.Mvc.Web` so AI Chat and Chat Interactions now stream data-source references with resolved links. The MVC sample registers a keyed article citation link resolver, exposes anonymous `/articles/{id}` display pages for citation targets, and preserves citation metadata so Chat Interactions can render linked citations during streaming and after history reloads.
- Added chat-session analytics parity to `CrestApps.Mvc.Web`, introduced shared extracted-data snapshot recording through `IAIChatSessionExtractedDataRecorder`, added Orchard Core and MVC extracted-data reports with required profile/date filtering plus CSV export, and reduced the MVC chat-session close background service cadence to every 5 minutes so post-session analytics and extracted-data reports stay current.
- Added a shared `IAIDataSourceIndexingService` plus MVC queue/handler wiring so article-backed data sources now rebuild their knowledge-base indexes on create, update, delete, scheduled sync, scheduled alignment, and manual **Sync** requests. MVC article changes now propagate into data-source knowledge-base indexes automatically, and strict `IsInScope` prompts now explicitly forbid offering general-knowledge fallback answers when no grounded data is available.
- Fixed MVC citation rendering after chat-history reload so serialized citation metadata keeps its resolved article labels and URLs, and citation links now open in a new browser tab in both AI Chat and Chat Interactions.
- Replaced the shared chat-document authorization interface with ASP.NET Core resource authorization handlers, split the OpenXml and PDF readers into dedicated `CrestApps.AI.OpenXml` and `CrestApps.AI.Pdf` framework packages, and updated MVC upload surfaces to show supported file extensions from `ChatDocumentsOptions` so profile knowledge uploads stay limited to embeddable formats while chat uploads reflect every registered reader.
- Split `RagTextNormalizer` and its Markdig-backed normalization dependencies into `CrestApps.AI.Markdown`, kept that package fully opt-in so `AddCrestAppsAI()` no longer registers Markdown support automatically, and moved the shared MCP FTP/SFTP handlers into dedicated `CrestApps.AI.Ftp` and `CrestApps.AI.Sftp` packages so `CrestApps.AI` and `CrestApps.AI.Mcp` no longer carry those optional transport-specific dependencies directly. MVC and Orchard Core now opt into those packages through explicit registration extensions.
- Moved the data-extraction user prompt onto a real AI template (`data-extraction-prompt`) so the framework no longer hand-assembles that prompt in code, and kept the Lucene analyzer dependency explicit on `CrestApps.AI` where `LuceneTextTokenizer` actually lives.
- Simplified the General AI settings editors in Orchard Core and MVC by removing the separate override switches for maximum iterations, distributed caching, and OpenTelemetry. Those saved values now apply directly, while `AbsoluteMaximumIterationsPerRequest` remains the configuration-owned safety cap.
- Changed the MVC AI Profile and profile-source AI Template defaults so **Restrict answers to retrieved data only** now starts disabled, and added a **Display** action to the MVC Articles list with a wider actions column for the extra button.
- Fixed MVC AI Profile document knowledge sync so attached profile documents now resolve embeddings from the profile's own deployment scope before falling back to the global embedding deployment, update the AI Documents index when files are added or removed, and clean up indexed chunks when a profile is deleted.
- Consolidated AI Profile and chat-interaction memory flags onto a single shared `MemoryMetadata` shape, migrated Orchard-stored memory settings to the new metadata key, and kept MVC legacy memory values readable while new saves now write the unified metadata model. MVC cookie-authenticated users still expose a stable identity to the memory tools via `NameIdentifier` with a `Name` fallback, so `save_user_memory` remains available for both AI Profiles and Chat Interactions.
- Moved the MVC AI Settings memory toggles into the **Memory** card, strengthened the shared memory-availability prompt so durable facts such as names and preferences trigger `save_user_memory` more reliably, and added MVC memory indexing on `AIMemoryEntry` catalog writes so saved memories are pushed into the configured AI Memory search index for later retrieval.
- Moved the `document-context-header` prompt into the shared framework template set so document preemptive RAG resolves the same prompt in both Orchard Core and `CrestApps.Mvc.Web` instead of depending on an Orchard-only embedded resource.
- Fixed MVC SQLite lock failures during memory retrieval by keeping `AIMemorySearchService` on the current scoped YesSql session instead of creating a nested DI scope and second SQLite session while document RAG was still active.
- Hardened `save_user_memory` so when the AI sends a `description` that just repeats the memory `content`, the tool now regenerates that description from the stable `name` instead. This keeps memory metadata category-oriented for indexing while leaving `content` as the embedded value.
- Tightened the MVC sample's shared AI editing/runtime parity again: MVC data sources now reject `AI Documents`, `AI Memory`, and `Data Source` source-index profiles on both the dropdown and server-validation paths; MVC admin settings now expose the full shared `GeneralAISettings` surface including preemptive memory retrieval and override flags; profile and profile-template editors now merge extraction and analytics into a shared **Data Processing & Metrics** tab; and data-source orchestration now uses a dedicated shared `DataSourceOrchestrationHandler` plus `data-source-availability` prompt so documents, memory, and data sources compose independently instead of coupling source instructions together.
- Hardened MVC chat-interaction document uploads so the request no longer waits on host-specific AI-document indexing work. Uploads now persist the document immediately, queue the MVC indexing work in the background, and log any later indexing failures without taking down the upload flow or the sample app.
- Excluded `src/Startup/CrestApps.Mvc.Web/App_Data/**` from `.NET 10` `dotnet watch` inputs so Aspire and other watch-based local runs no longer restart `MvcWeb` when chat document uploads create files under `App_Data/Documents` or runtime services update local app-data files.
- Fixed the MVC data-source hosted services so periodic timer cancellation is treated as normal shutdown behavior, and corrected `DataSourceAlignmentBackgroundService` to handle empty `IAIDataSourceStore` results without null-dereferencing the collection during alignment.
- Reorganized the Orchard Core AI Profile editor so the profile-level data source selector, RAG controls (`Strictness`, retrieved documents, in-scope answers only, and filter), profile documents, and session-document uploads now all live under a single **Knowledge** tab instead of splitting data source settings across the General, Parameters, and Documents sections.
- Completed the remaining parameter-help consistency work so MVC AI Profiles, MVC profile-source AI Templates, Orchard Core AI Profiles, and Orchard Core profile-source AI Templates now all show the same `(?)` tooltip help on model parameters as Chat Interactions.
- Fixed the MVC **AI Templates** navigation links so the menu and dashboard now route to the actual `AITemplateController` instead of the obsolete `Template` controller name that returned 404.
- Registered `IAIProfileStore` in the Orchard Core AI core service extension so migrations and other Orchard-hosted profile services can resolve the shared profile store through DI consistently with the framework host.
- Fixed the remaining Orchard Core chat-interaction editor cleanup by moving data source + RAG settings and document retrieval settings into the shared **Knowledge** tab, renaming the document retrieval label to **Document Top N**, and registering `DefaultSpeechVoicePresenter` in Orchard Core AI core services so the profile chat-mode drivers resolve cleanly.
- Added MVC AI settings parity for AI Memory, data-source defaults, default orchestrator preemptive RAG, and chat-interaction memory defaults; completed the missing Knowledge-tab tooltip help in the MVC chat UI; registered the missing MVC deployment manager needed by the settings page; and hardened the PR #367 CodeQL paths around logging, file-path validation, and MCP transport/auth normalization.
- Extended the Orchard Core legacy AI document-type migration so existing tenants rerun the Orchard-to-framework type rewrite and recover legacy `AIProfile`, `AIChatSession`, and `AIChatSessionPrompt` rows that would otherwise fail YesSql type resolution.
- Renamed the reusable prompting layer to `CrestApps.Templates`, updated registration to `AddTemplating()` / `AddTemplatesFromAssembly()`, and switched file-system plus embedded-resource discovery from `AITemplates/...` to `Templates/...`.
- Added a new `AddAITemplating()` helper in `CrestApps.AI` so AI hosts can register the shared templating engine plus the built-in `SystemPrompt` and `Profile` template sources with a single call.
- Continued the MVC feature reorganization by relocating feature-specific hubs, minimal API endpoints, background services, indexes, and the `_ChatWidget` partial into their matching area folders.
- Moved the shared MVC `_CopilotConfig` partial into `Views/Shared` so AI and ChatInteractions area views resolve the Copilot configuration panel through the standard Razor shared-view lookup path instead of failing at runtime outside the Admin area.
- Removed the legacy CrestApps-owned text-to-speech abstraction surface so AI client factories and providers now use `Microsoft.Extensions.AI.ITextToSpeechClient` and the built-in `TextToSpeech*` models directly.
- Fixed the remaining AI admin consistency gaps across Orchard Core and `CrestApps.Mvc.Web`: MVC settings now reload the just-saved values on the first redirect, MVC AI Profiles and Profile-source AI Templates now align on **Instructions** / **Knowledge** organization (including prompt templates, user memory, and RAG fields), Chat Interactions place **Knowledge** before **Capabilities**, the AI Templates list keeps action buttons on one line, the admin chat widget only renders when a profile is selected in site settings, and Orchard Core AI Core now registers `ISpeechVoiceResolver` so the AI Profile editor resolves speech voice presenters correctly.
- Fixed the MVC AI Profile and AI Template profile-type toggle scripts so changing enum-backed profile type dropdowns now correctly shows and hides the matching sections instead of comparing numeric option values to CSS class names.
- Normalized MVC/framework Azure OpenAI provider names so `AzureOpenAI` configuration entries and persisted selections resolve through the same runtime provider key as existing `Azure` registrations, fixing embedding deployment resolution for MVC index profile creation.
- Fixed Orchard Core AI Profile and AI Template display driver placements so data source, RAG controls, profile documents, session documents, and user memory all render under a dedicated **Knowledge** tab (`%Knowledge;2`) instead of appearing as collapsed cards (`#Knowledge`) inside the default tab.
- Fixed Orchard Core Chat Interaction editor tab ordering so the Knowledge tab renders second (`;2`) and Capabilities renders third (`;3`), matching the AI Profile tab layout.
- Added A2A host support to `CrestApps.Mvc.Web` — exposes AI Agent profiles via the Agent-to-Agent protocol with an inline task manager, `/.well-known/agent-card.json` endpoint, `/a2a` protocol endpoint, and authentication middleware (None, API Key, or Cookie-based).
- Created `CrestApps.Mvc.Samples.A2AClient` sample project demonstrating A2A client connectivity to `CrestApps.Mvc.Web`, with agent discovery and streaming message support.
- Created `CrestApps.Mvc.Samples.McpClient` sample project demonstrating MCP client connectivity to `CrestApps.Mvc.Web`, with tools, prompts, resources, and resource template browsing.
- Created a shared `WithCrestAppsHandlers()` extension in the `CrestApps.AI.Mcp` framework that registers all standard MCP server handlers (ListTools, CallTool, ListPrompts, GetPrompt, ListResources, ListResourceTemplates, ReadResource), eliminating ~150 lines of duplicated handler code between Orchard Core and `CrestApps.Mvc.Web`.
- Updated Orchard Core `IMcpServerPromptService` and `IMcpServerResourceService` to extend their framework counterparts, enabling seamless shared handler resolution without adapter classes.
- Extracted all YesSql store registration, index providers, catalog managers, schema creation, and seed data from `CrestApps.Mvc.Web` `Program.cs` into `YesSqlServiceCollectionExtensions` (`AddYesSqlDataStore()`, `InitializeYesSqlSchemaAsync()`, `SeedArticlesAsync()`), significantly reducing `Program.cs` noise.
- Added `CrestApps.Mvc.Samples.A2AClient` and `CrestApps.Mvc.Samples.McpClient` to the Aspire AppHost with proper references, dependency ordering, and environment variables that disable authentication on the MVC.Web MCP server and A2A host for local testing.
- Fixed a crash in the MVC.Web AI Profile file upload that could bring down the entire host when processing certain document formats. Added per-file try-catch in `UploadDocumentsAsync` and defensive error handling in `DefaultAIDocumentProcessingService.ProcessFileAsync` around the document reader invocation.
- Aligned the AI Template editor (Profile source) with the AI Profile editor in MVC.Web: the Knowledge tab now includes Enable user memory, Data Source (RAG data source dropdown, Strictness, Retrieved documents, Restrict answers, Filter), document index alerts, Allow session documents, and Document Top N — matching the AI Profile UI exactly.
- Standardized tab ordering across AI Template Create and Edit views to match the AI Profile editor: Basic Info → AI Parameters → Capabilities → Knowledge → Data Extraction → Analytics → Settings.
- Moved the "Enable user memory" option from the Basic Info tab to the Knowledge tab in the AI Profile Create view for consistency with the Profile Edit view and AI Template views.
- Added document management to AI Profile Edit view: users can remove existing documents and add new files, with proper cleanup of document chunks and index entries on removal.
- Simplified file upload UX in AI Profile Create view: removed the "+ Add" button; files are now added automatically when selected via the file input.
- Made the Title field required with a red asterisk across all AI Profile and AI Template Create/Edit views.
- Prompt Subject field now shows for both Chat and TemplatePrompt profile types across all Profile and Template views.
- Removed duplicate "Enable user memory" from the Basic Info tab in AI Template Create (already present in Knowledge tab).
- Made the Description field Agent-only in AI Template Create/Edit views, matching the AI Profile editor behavior.
- Added per-task Capabilities tabs to post-session processing task cards in all AI Profile and AI Template views. Each task card now has "Task Info" and "Capabilities" tabs, where the Capabilities tab allows selecting AI Agents, A2A Hosts, MCP Hosts, and AI Tools specific to that task.
- Extended the `PostSessionTask` framework model with `ToolNames`, `AgentNames`, `A2AConnectionIds`, and `McpConnectionIds` properties to support per-task capability configuration.
- Updated MVC.Web post-session task cards so the Options textarea is only shown for `PredefinedOptions` tasks and stays hidden for `Semantic` tasks, including when users change the task type after the card is already rendered.
- Fixed MVC.Web follow-up regressions: AI Profile post-session task controls now appear again when enabled, AI Profile Edit backfills attached documents from the document store so previously uploaded files render for removal, AI Template Create now shows Prompt templates in the AI Parameters tab, and the template profile-type show/hide logic now re-runs correctly when switching to the Profile source or changing profile type.
- MVC AI profiles now persist attached knowledge documents reliably after save, so reopening a profile shows previously uploaded documents and allows removing them later.
- Shared document orchestration now respects the "Restrict answers to retrieved data only" setting when document context is involved, preventing fallback to general knowledge in strict mode.
- Orchard Core data-source RAG registration now goes through the shared framework extension so MVC and Orchard use the same preemptive-RAG pipeline.
