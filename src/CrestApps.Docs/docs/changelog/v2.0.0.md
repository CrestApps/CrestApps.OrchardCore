---
sidebar_label: 2.0.0 Release Notes
sidebar_position: 1
title: "Version 2.0.0 Release Notes"
description: Module-first release notes for CrestApps.OrchardCore 2.0.0, including breaking changes, feature additions, and migration guidance from v1.x.
---

# Version 2.0.0 Release Notes

Version `2.0.0` is the major upgrade from the `v1.x` line to the current `v2` platform. It moves CrestApps.OrchardCore to Orchard Core 3 on .NET 10, expands the solution into a broader modular platform, and reshapes how AI deployments, chat experiences, retrieval, external integrations, and omnichannel communication are modeled.

This page is organized by module and feature so related changes stay together. Breaking changes, migration notes, and follow-up fixes are listed under the module or platform area they affect.

## Highlights in 2.0.0

- **Platform upgrade** to `.NET 10` and the Orchard Core `3.0.0` line used by this repository
- **Expanded AI platform** with deployments, provider connections, orchestrators, tools, AI Documents, AI Memory, MCP, A2A, Claude, and Copilot integrations
- **Broader Orchard module surface** including Content Access Control, Content Transfer, phone-number support, DNC Registry, Omnichannel Management, Recipes, SignalR, Roles, and Time Zones
- **Provider-agnostic telephony** with a soft phone widget, a SignalR hub, and a DialPad provider integration
- **Provider-agnostic retrieval architecture** built around AI Data Sources, AI Documents, and AI Memory instead of provider-bound BYOD flows
- **Canonical Docusaurus documentation** under `src\CrestApps.Docs`

## Recommended migration path

Use this order when moving a tenant or solution from `v1.x` to `v2.0.0`:

1. Back up every tenant database and export any important recipes or deployment plans.
2. Install the `.NET 10` SDK and move application projects to `net10.0`.
3. Update package feeds and package references to the `v2.0.0` line.
4. Review custom code for moved namespaces, shared `CrestApps.Core` packages, and updated async interface signatures.
5. Start the application once and allow the data migrations to complete.
6. Review AI provider connections, AI deployments, default deployment settings, AI profiles, and chat data in the admin UI.
7. Rebuild and validate tenant features such as AI chat, data sources, documents, MCP, Omnichannel imports, and custom modules.

## Before you upgrade

Before upgrading from `v1.x` to `v2.0.0`, back up every tenant database and review your custom code, package references, enabled features, and configuration.

At a high level, the platform changes are:

| Area | v1.x | v2.0.0 |
| --- | --- | --- |
| .NET SDK | `8.0.100` | `10.0.100` |
| Target frameworks | `net8.0;net9.0` | `net10.0` |
| Orchard Core | `2.1.x` line | `3.0.0-preview` line |
| NuGet feeds | NuGet only | NuGet + Orchard Core preview + CrestApps Core preview |
| Shared code model | Mostly repo-local core projects | Shared `CrestApps.Core` packages plus Orchard integration modules |
| AI deployments | Embedded provider and deployment selectors | First-class typed deployment records |
| Retrieval architecture | Mostly Azure OpenAI BYOD-centric | Provider-agnostic Data Sources, Documents, and Memory modules |
| Documentation | README-driven | Docusaurus documentation under `src\CrestApps.Docs` |

## Module and feature changes

### Artificial Intelligence

#### Core platform

- The AI platform now centers around first-class deployment records, provider connections, centralized defaults, and more source-agnostic editors and settings.
- `v2.0.0` replaces older inline or name-based deployment selectors with typed deployment records. Profiles, chat interactions, templates, and related settings now resolve deployments through the current deployment model.
- Deployment migration now imports legacy `DictionaryDocument<AIDeployment>` records, infers missing deployment types from legacy selectors, backfills default deployment site settings, merges duplicate legacy records when possible, and creates unique migrated records instead of mutating read-only configuration deployments.
- Deployment editors now load connection choices from the provider-connection catalog, provider-specific pickers show only the connections that belong to the selected provider, and deployment documents now persist the provider connection `Name` in `ConnectionName`.
- The deployment model now uses **Purpose** instead of **Type**. The admin UI reflects that rename, the migration persists converted values through `UpdateFrom6`, and a new `Vision` purpose is available in the default deployment settings and chat surfaces.
- The shared Orchard-facing AI code now uses `IClock` consistently for timestamps, and the Orchard integration now defers to the shared `CrestApps.Core.AI.Handlers.AIProfileHandler` instead of keeping duplicate local profile validation handlers.
- AI administration is reorganized in the v2 line. Settings, profile flows, and deployment defaults are grouped differently than in `v1.x`, and operator guidance should be updated accordingly.
- The AI configuration section now uses the nested `OrchardCore:CrestApps:AI` path. The old `CrestApps_AI` paths still work, but they log deprecation warnings and should be treated as transitional.

#### A2A

- `v2.0.0` adds Agent-to-Agent support so Orchard-hosted AI solutions can participate in broader multi-agent workflows.
- A2A host authentication now defaults to `OpenId` when no authentication type is configured, so anonymous access must be chosen explicitly.
- Shared OAuth 2.0 editors in the A2A SSE configuration paths now render `Token Endpoint`, `Client ID`, and `Scopes` only once, which preserves posted values and avoids validation failures during save.
- See [A2A](../ai/a2a/index.md).

#### Agent

- The AI platform now supports orchestrator-based execution, allowing tools, retrieval, templates, agents, and external integrations to participate in coordinated flows instead of relying on a simpler prompt-routing model.
- The `createOrUpdateContentItem` tool now resolves corrective payload guidance and payload validation through a swappable service, keeps the dropped-value detection after `ContentItem` mapping, and can use recipe-backed content-type schemas when the Recipes feature is enabled.
- AI content-item tools now instruct models to create the top-level content item once and embed nested `BagPart`, `FlowPart`, and widget-style child items in the same payload instead of issuing separate tool calls.
- Recipe-backed AI content-item validation now evaluates the authored schema as-is, helper-tool dependencies can be declared during tool registration, and the AI tooling automatically includes `getContentItemSchema` and `getOrchardCoreRecipeJsonSchema` where appropriate.
- The `getContentItemSchema` and `getOrchardCoreRecipeJsonSchema` flows now expose richer schema discovery, preserve the root `steps` array behavior, keep the full step-name enum available, and report valid step names when an unknown step is requested.
- See [Artificial Intelligence Suite](../ai/index.md), [AI Agent](../ai/agent.md), and [AI Tool Management](../ai/tools.md).

#### Chat

- The base AI chat experience is broader in `v2.0.0`, with richer session analytics, improved admin and frontend widget scenarios, more deployment-aware and profile-aware flows, and stronger post-session processing support.
- The AI chat admin UI, admin widget, frontend widget, and chat interaction surfaces now render `[doc:N]` markers as superscript citations with linked reference lists when the underlying references can be resolved.
- Orchard-hosted chat surfaces now use the rebuilt bundled scripts and styles from the repository instead of older pinned CDN artifacts.
- Playback and conversation controls only appear when the required speech deployments are actually available for the tenant.
- The **Prompt subject** editor only appears for **Template generated prompt** profiles and profile-source templates.
- The frontend widget now removes theme-added paragraph bottom spacing inside rendered messages so wrapped `<p>` blocks do not create extra gaps.
- The shared `list-management-ui` script now registers `window.listManagementUI` explicitly so admin catalog pages such as **Chat Interactions** initialize reliably.
- The admin AI chat session page now keeps the placeholder element rendered even when a historical session already has messages, which prevents the shared chat UI runtime from aborting initialization when users reopen an existing session from history.
- See [AI Chat](../ai/chat.md) and [AI Chat Analytics](../ai/chat-analytics.md).

#### Chat.Claude

- `v2.0.0` adds Claude support as a first-class Orchard integration with provider-specific settings and Claude-oriented deployment and model selection support.
- See [Claude](../ai/claude.md).

#### Copilot Integration

- The new Copilot integration brings a GitHub Copilot-style chat experience into Orchard Core while still allowing Orchard-specific tools, content access, and orchestration behavior.
- Copilot site settings now include a **Not configured** authentication mode so administrators can disable the orchestrator without deleting previously stored credentials.
- GitHub OAuth client ID and client secret validation only run when that authentication mode is selected, and an existing stored secret remains valid until it is explicitly replaced.
- The **Settings -> Artificial Intelligence** page now keeps the memory guidance in the matching **Memory** section context, and the AI settings cards render in a stable order so **Admin Widget**, **Copilot**, and **Claude** appear together predictably.
- See [AI Chat Copilot Integration](../ai/copilot.md).

#### Chat.Interactions

- `v2.0.0` adds ad-hoc chat interactions that do not require a predefined AI profile for every scenario.
- These interactions support more dynamic admin and frontend AI experiences plus richer file, image, and interaction-driven workflows.
- Chat interaction surfaces now respect the current document-upload and image-upload settings so each UI exposes only the extensions allowed for the tenant and profile.
- See [AI Chat Interactions](../ai/chat-interactions.md).

#### Data Sources

- AI Data Sources are now a first-class, provider-agnostic capability instead of an Azure OpenAI BYOD-only story.
- The Orchard integration now bridges `IDocumentIndexHandler` source-index notifications into an Orchard-managed `IAIDataSourceIndexingQueue`, batching request-scoped work and dispatching it after the request so source-index changes refresh mapped knowledge-base indexes automatically.
- Orchard-specific queued sync work now overrides `IAIDataSourceIndexingService` correctly before background work is dispatched, which keeps re-syncs on the Orchard adapter path.
- Synchronizing a source index profile now queues a full re-sync for every mapped AI data source, and the knowledge-base profile is resolved consistently by profile `Name` during that work.
- The AI Data Sources module now adds a dedicated `AIDataSourceIndex` migration that backfills legacy `DictionaryDocument<AIDataSource>` records, supports older `CrestApps.AI` and `CrestApps.Core.AI` type names, repairs missing `ProfileSource` values, infers source-field mappings, assigns or creates knowledge-base profiles as needed, and normalizes embedding selectors to the canonical deployment record names.
- The normalization pass now also accepts legacy Azure provider aliases, strips old `Azure-` prefixes where needed, and repairs tenants where deployment import completed after earlier index-profile normalization.
- The Data Sources UI now groups **Source Index** and **Knowledge Base Index** choices under localized provider names, filters the **Source Index** selector to exclude AI-managed index types, and fixes name filtering so display-text searches behave correctly.
- See [AI Data Sources](../ai/data-sources/index.md).

#### AI Documents

- `v2.0.0` adds a dedicated AI document pipeline for upload, parsing, chunking, indexing, and retrieval.
- The document story now includes PDF and OpenXML support plus dedicated storage and indexing integrations.
- Legacy AI documents stored under earlier `CrestApps.OrchardCore.AI.*` or `CrestApps.AI.*` document type names are rewritten to the current shared model names so older uploaded profile and session documents remain readable and removable.
- AI Documents settings now expose the default **Document retrieval mode**, and the document-related profile, template, and interaction editors use localized retrieval-mode choices.
- Managed mapping normalization now keeps built-in Azure AI Search document index fields such as `Content` and `Embedding` from being duplicated repeatedly on later edits.
- See [AI Documents](../ai/documents/index.md), [PDF support](../ai/documents/pdf.md), [OpenXML support](../ai/documents/openxml.md), and [Azure Blob Storage](../ai/documents/azure-blob-storage.md).

#### MCP

- The platform now supports both exposing Orchard Core as an MCP server and consuming external MCP servers as a client.
- `v2.0.0` also expands the MCP surface with FTP and SFTP resource modules.
- MCP API key authentication now uses constant-time secret comparison.
- MCP host authentication now defaults to `OpenId` when no authentication type is configured, so anonymous access must be chosen explicitly.
- Shared OAuth 2.0 editors in MCP SSE configuration paths now render `Token Endpoint`, `Client ID`, and `Scopes` only once, which preserves posted values and avoids validation failures during save.
- See [Model Context Protocol](../ai/mcp/index.md), [MCP Server](../ai/mcp/server.md), [MCP Client Integration](../ai/mcp/client.md), [FTP MCP resources](../ai/mcp/ftp.md), and [SFTP MCP resources](../ai/mcp/sftp.md).

#### Memory

- `v2.0.0` adds persistent AI memory support so assistant behavior can retain context across sessions and use provider-backed retrieval or indexing strategies.
- The user-profile memory editor now uses Orchard Core's standard destructive confirmation flow when clearing saved memory and relies on permission-aware authorization for self-service and delegated clearing.
- Managed mapping normalization now keeps built-in Azure AI Search AI Memory fields from being duplicated on later edits.
- The AI Memory index-profile editor now treats the embedding deployment as a creation-time choice only, showing it as read-only for existing profiles because migrations normalize older data ahead of time.
- See [AI Memory](../ai/memory.md).

#### Prompting

- Prompt templates and profile templates are now first-class reusable building blocks in the AI platform.
- This makes tenant-level AI experiences more reusable and more consistent across chat, interactions, and automation scenarios.
- See [AI Prompt Templates](../ai/prompt-templates.md) and [AI Profile Templates](../ai/profile-templates.md).

#### Response handlers

- Chat handling is now extensible through pluggable response handlers.
- This enables AI-driven responses, deferred external processing, live-agent handoff, and hybrid AI and human support scenarios.
- See [Response Handlers](../ai/response-handlers.md) and [Chat UI Notifications](../ai/chat-notifications.md).

### Contact Center

#### New contact center orchestration layer

- `v2.0.0` introduces the `CrestApps.OrchardCore.ContactCenter` feature, the base of a new contact center orchestration layer that sits between the Omnichannel CRM and the Telephony module.
- The contact center owns orchestration while the CRM owns business work data and Telephony owns media execution. `OmnichannelActivity` remains the universal work item for queues, dialers, wrap-up, disposition, and workflows.
- Omnichannel activities now include Contact Center classification and assignment metadata: activity kind, activity source, assignment status, reservation id, reserved-by actor, reservation time, and reservation expiry. This lets activities exist without an owner until a queue or dialer safely reserves and assigns them.
- This release adds the `Interaction` communication-history aggregate and store, a durable, append-only domain **event log** (`InteractionEvent`) with idempotency support, an `IContactCenterEventPublisher` that records events and dispatches them to decoupled handlers, the source-neutral `IActivityDispositionService` contract, and baseline permissions.
- Contact Center now defines optional `IContactCenterVoiceProvider` abstractions for PBX providers that can participate in dialer dialing, provider-side call assignment, provider-side queue placement, queue events, and PBX presence synchronization while Contact Center remains the routing/dialer brain.
- The feature is delivered as `CrestApps.OrchardCore.ContactCenter.Abstractions`, `CrestApps.OrchardCore.ContactCenter.Core`, and the `CrestApps.OrchardCore.ContactCenter` module. Additional capabilities (queues, routing, presence, voice, dialer, wrap-up, supervision, and analytics) are delivered in later phases.
- See [Contact Center](../contact-center/index.md).

#### Agents, queues, reservations, and voice-routed dialing

- Adds the `CrestApps.OrchardCore.ContactCenter.Agents` feature: agent profiles, presence states (offline/available/reserved/busy/wrap-up/break), capacity, skills, and queue/campaign sign-in through the Telephony soft phone.
- Adds the `CrestApps.OrchardCore.ContactCenter.Queues` feature: work queues, queue items, the reservation lifecycle, and availability-based assignment that pairs the highest-priority waiting activity with the longest-idle available agent. A background task expires stale reservations and assigns queued work every minute.
- Adds the `CrestApps.OrchardCore.ContactCenter.Dialer` feature: dialer profiles, dialing modes, pacing, and dialer activity batches. Outbound dialing now routes voice calls through the Voice Contact Center Call Router and `IContactCenterVoiceProvider`, so the Contact Center owns assignment, queue, pacing, and compliance while provider modules execute calls.
- Adds the `CrestApps.OrchardCore.DialPad.Dialer` feature as the DialPad implementation of the Contact Center voice provider boundary over the existing DialPad telephony provider.
- Adds the first routing strategy layer: required queue skills filter eligible agents, longest-idle scoring selects among eligible candidates, and each assignment publishes an auditable routing-decision event with candidate scores and reasons.
- Adds `IContactCenterVoiceProviderResolver` so PBX/voice providers that implement Contact Center queueing or call-assignment operations can be resolved consistently by technical name.
- Contact Center management entries now live under **Interaction Center**. Adds a managed **Skills** CRUD screen, and queue and dialer profile CRUD screens follow the Omnichannel Campaigns display-driver UI pattern with root edit wrappers.
- Telephony soft-phone extensibility now uses `DisplayDriver<SoftPhoneWidget>` contributed tabs/views/header actions. Contact Center injects a **Work** tab for queue/campaign sign-in and sign-out, plus a soft-phone header presence dropdown with system-approved **Request break** behavior. Routing skills are administrator-owned eligibility data and are no longer self-selected by agents from the soft phone.
- See [Agents, Queues & Dialer](../contact-center/agents-queues-dialer.md).

#### Inbound voice routing and the incoming-call modal

- Adds the `CrestApps.OrchardCore.ContactCenter.Voice` feature and the Voice Contact Center Call Router. Inbound provider calls are normalized into a CRM activity (`Kind = Call`, `Source = Inbound`) with its Subject, recorded as a voice `Interaction`, enqueued, routed to the longest-idle available agent signed in to the inbound queue, and offered through the Telephony soft-phone incoming-call modal; outbound dialer calls also route through this voice boundary.
- Queues gain an optional inbound channel endpoint mapping (`InboundChannelEndpointId`) so a dialed number routes to a specific queue; a single unmapped enabled queue is used as the default inbound queue.
- A provider-agnostic ingress endpoint (`POST /api/contact-center/voice/inbound`, requiring `Manage interactions`) accepts a normalized `InboundVoiceEvent`; provider webhooks can also call `IVoiceContactCenterCallRouter` directly.
- Implements the source-neutral `IActivityDispositionService` and routes CRM activity completion through it, so inbound and outbound calls are dispositioned through the same Subject workflow.
- See [Inbound voice](../contact-center/index.md#inbound-voice).

#### Inbound entry points (Phase 8)

- Adds a reusable **inbound entry point** catalog under **Interaction Center → Entry points** (Voice feature). An entry point maps one or more dialed numbers (DIDs) to a target queue and priority, gates the call with a business-hours calendar, and defines a **closed action** (hold in queue, voicemail, overflow, or reject) with welcome/closed messages.
- `IEntryPointResolver` matches the dialed number to an entry point and, using the business-hours calendar, produces an `EntryPointRoutingPlan` (open/closed, effective queue, priority). The Voice Contact Center Call Router now consults the resolver first: open calls route to the entry point's queue at its priority; closed calls hold, overflow, or are sent to voicemail/rejected per the closed action. When no entry point matches, routing falls back to the existing endpoint-to-queue mapping.
- See [Inbound entry points](../contact-center/agents-queues-dialer.md#queues-reservations-and-assignment).

#### Voice foundation hardening: media delivery, call sessions, and normalized provider events

- Voice providers now declare a **delivery model** (`VoiceProviderDeliveryModel`): `AgentDeviceNative` (the provider rings the agent's own device, like DialPad's WebRTC soft phone) or `ServerSideAcd` (the provider parks the call and the Contact Center bridges it to the agent). `IContactCenterVoiceProvider` gains a `DeliveryModel` property and a `ConnectToAgentAsync` operation, plus an `AgentConnect` capability flag.
- Adds `IContactCenterCallCommandService`, a single authoritative server-side command for the agent offer lifecycle. Accepting an offer now accepts the reservation, connects the media to the agent for server-side ACD providers, and advances the interaction and call session together, so the orchestration state and the provider media state can no longer diverge. The result reports whether the agent's device must still answer the media (`RequiresDeviceAnswer`). The Contact Center `VoiceController` accept/decline actions delegate to this command.
- Adds the `CallSession` aggregate: the Contact Center's business-oriented projection of a voice call, mapping a provider call to an interaction, agent, and queue and tracking the normalized call lifecycle and talk/hold durations without owning media execution.
- Adds normalized provider voice events: `ProviderVoiceEvent` and `IProviderVoiceEventService` ingest provider/PBX call-state changes, match them to the interaction and call session by provider call id, advance state and timestamps, bridge the agent for answered outbound calls on server-side ACD providers, and publish Contact Center domain events. Events carrying an already-seen idempotency key are ignored, so duplicate or out-of-order webhook deliveries are safe.
- See [Voice provider integration](../contact-center/index.md#voice-provider-integration).

#### Assignment safety: capacity enforcement and concurrency-safe assignment

- Agent capacity is now enforced during routing. A capacity routing strategy counts each candidate agent's active (not ended and not failed) interactions and rejects agents that have reached their configured `MaxConcurrentInteractions`, so an agent is never offered more concurrent interactions than they can handle.
- Assignment for a queue now runs under a per-queue distributed lock. Two nodes — or the reservation-expiry background task running alongside an inbound call — can no longer double-assign the same queue item or reserve the same agent twice.
- Adds a transfer/conference taxonomy. `IContactCenterTransferService` records blind or consultative transfers to an agent, queue, external destination, or entry point on the interaction's transfer history, publishes `InteractionTransferred`, and re-enqueues the activity for queue transfers; `ContactCenterVoiceProviderCapabilities` gains `CallTransfer` and `Conference` flags so provider modules can advertise media handoff support.
- See [Queues, reservations, and assignment](../contact-center/agents-queues-dialer.md#queues-reservations-and-assignment).

#### Routing depth: routing policy, sticky agent, SLA aging, business hours, and overflow

- Each queue now selects a primary **routing strategy**: *Longest idle* (default), *Round robin* (offers to the agent who least recently received an assignment, tracked on `AgentProfile.LastAssignedUtc`), or *Least busy* (offers to the agent handling the fewest active interactions). Only the selected strategy scores candidates; skill and capacity filtering still run first, and assignment still publishes an auditable routing-decision event.
- Queues can **prefer the sticky agent**. The activity's assigned user is captured on the queue item when it is enqueued, and routing boosts that agent when they are eligible and available, so returning work prefers the agent the customer already worked with. The preference is additive and never overrides eligibility.
- Queues can enable **SLA aging**, which raises a waiting item's effective priority by one step for every SLA-threshold interval it waits past the threshold, so aging work is routed ahead of newer higher-priority work instead of starving.
- Adds a reusable **business-hours calendar** catalog under **Interaction Center → Business hours** (Queues feature). A calendar defines a time zone, a weekly open window per day, and all-day holiday dates. A queue can reference a calendar; while it reports the queue closed, assignment pauses, and the queue's **after-hours action** either holds items or overflows them.
- Adds queue **overflow**: a queue can set an overflow queue and an overflow-after threshold so items waiting too long (or all items while the queue is closed and set to overflow) move to a broader team's queue. Overflow runs each minute alongside reservation expiry and assignment.
- See [Routing policy](../contact-center/agents-queues-dialer.md#routing-policy) and [Business hours and overflow](../contact-center/agents-queues-dialer.md#business-hours-and-overflow).

#### Subject Flow required-disposition policy

- A subject flow can now mark a subject as **requiring a disposition** (`SubjectFlowSettings.RequireDisposition`, edited on the **Configure** screen). When set, an activity using that subject cannot be completed until a disposition is selected.
- The rule is enforced centrally in `IActivityDispositionService`, so it holds for every completion path — the CRM completion screen, dialer, and automation — and for both inbound and outbound work. Completion now also skips Subject Actions when no disposition is selected instead of failing.
- This keeps the Subject Flow as the single decision controller for inbound and outbound work; a separate contact-center wrap-up concept is not needed.
- See [Subject Flow is the single decision controller](../contact-center/index.md#subject-flow-is-the-single-decision-controller).

#### Dialer safety: per-mode strategies and an outbound compliance gate

- Each automated dialing mode is now a dedicated `IDialerStrategy` resolved by mode, so unsupported modes are withheld instead of falling through to an unsafe default. **Predictive dialing is disabled**: the editor hides it, saving a Predictive profile is rejected, and the dialer refuses to run it until answer-rate forecasting and abandonment controls exist. **Power dialing is hard-capped** at a safe number of calls per agent (`PowerDialerStrategy.MaxCallsPerAgent`).
- Adds `IDialerEligibilityService`, an outbound compliance gate that runs before every attempt and records an auditable `DialSuppressed` event when an attempt is blocked. It enforces a present destination, the attempt limit, the `RetryDelayMinutes` retry cool-down, the contact's do-not-call communication preference, a configurable calling window evaluated in the contact's local time zone, and any registered national do-not-call registry (for example USA FTC or Canada DNCL).
- Dialer profiles gain calling-window settings (*Enforce a calling window*, start/end local hour, and a default time zone). Do-not-call and registry suppressions cancel the activity, while calling-window and cool-down suppressions release the reservation and leave the activity available for a later cycle.
- The single-attempt logic moved into `IDialerAttemptService`, and `DialerService` now validates the profile and delegates pacing to the resolved strategy.
- Successful outbound dial attempts now accept the reservation before returning, so the reservation-expiry task cannot later release the agent or queue item while the provider call is still active.
- See [Dialer](../contact-center/agents-queues-dialer.md#dialer).

#### Callbacks (Phases 5/10)

- Adds a **callback** model. `CallbackRequest` records a scheduled call-back (destination, contact, campaign, optional queue/agent, requested and due times, status, attempts, notes). `ICallbackService.ScheduleAsync` records a pending callback and publishes `CallbackScheduled`; `PromoteDueAsync` (run every minute by the `CallbackDispatchBackgroundTask`) turns each due callback into an outbound CRM activity (`Kind = Call`, `Source = Callback`), enqueues it when a queue is set, marks it `Scheduled`, and publishes `CallbackPromoted`.
- See [Dialer](../contact-center/agents-queues-dialer.md#dialer).

#### Agent state reason codes

- Adds the **Agent states** catalog (`AgentStateReasonCode`) under **Interaction Center → Agent states** (Agents feature). A reason code has a unique name, description, the presence state it sets (`AppliesTo`), a sort order, and an enabled flag, and is managed with the same display-driver CRUD pattern as Skills and queues, gated by `ManageContactCenterAgents`.
- The soft-phone presence dropdown now lists the enabled reason codes (ordered by sort order); choosing one sets the agent's presence to the reason's `AppliesTo` state and records the reason on the agent profile and the `AgentPresenceChanged` event. When no reason codes exist, the built-in not-ready states are shown.
- The Agents feature seeds a standard set of reason codes at setup (short break, lunch, away from desk, team meeting, training, coaching, system issue) by executing the `agent-state-reason-codes` module recipe, and the `AgentStateReasonCode` recipe step makes reason codes importable and portable between tenants.
- See [Agent state reason codes](../contact-center/agents-queues-dialer.md#agent-state-reason-codes).

#### Real-time agent and supervisor layer

- Adds the `CrestApps.OrchardCore.ContactCenter.RealTime` feature (depends on **Queues** and the **SignalR** module): a dedicated SignalR `ContactCenterHub` for agent desktops, supervisor dashboards, and queue monitors. Agents join their own user group plus a group per signed-in queue; supervisors join a `cc:supervisors` group and can `WatchQueue`/`UnwatchQueue` individual queues for wallboards.
- Adds a live `AgentSession` aggregate, separate from the administrator-owned `AgentProfile`, that tracks an agent's open SignalR connections and last heartbeat. Splitting volatile live state from durable configuration means a closed browser no longer leaves an agent `Available`.
- The client sends a `Heartbeat` every 30 seconds; a per-minute background task signs out any agent whose heartbeat is older than 90 seconds and removes the session, so routing stops targeting a dead client. A brief disconnect (such as a page refresh) is tolerated by the grace window.
- The hub's `GetSnapshot` returns an `AgentDesktopSnapshot` combining the durable profile (presence, reason, queue/campaign membership, active reservation) with live session state, so a reconnecting desktop restores itself without replaying the event history.
- `ContactCenterRealTimeEventHandler` projects the durable domain event log onto the real-time layer, broadcasting presence changes, work offers (`OfferReceived`/`OfferRevoked`), and queue depth (`QueueStatsChanged`) to the affected agent, queue watchers, and supervisors. A `contact-center-realtime` client script resource (depending on `signalr`) wraps connection, heartbeats, snapshot retrieval, and the strongly-typed callbacks.
- Adds the `MonitorContactCenter` permission for supervisor real-time visibility, and a default **Supervisor** role stereotype.
- See [Real-time experience](../contact-center/index.md#real-time-experience).

#### Reliable event dispatch (outbox)

- Contact Center domain-event dispatch is now **at-least-once**. `IContactCenterEventPublisher` records the immutable `InteractionEvent` and hands it to the new `IContactCenterOutbox`, which runs handlers inline for low latency and, when a handler fails, persists a durable `ContactCenterOutboxMessage` instead of silently dropping the event.
- A per-minute `OutboxDispatchBackgroundTask` retries failed dispatches with exponential back-off (30s, 60s, 120s, … capped at 30 minutes) and **dead-letters** a message after ten failed attempts for inspection. A retry re-runs every handler, so handlers must be idempotent (the shipped handlers are, and provider-sourced events carry an idempotency key).
- Handler dispatch and the per-handler failure isolation moved out of the publisher into the outbox, so a transient failure in a downstream projection (SignalR, indexing, or an external service) no longer loses work.
- Adds signed **provider voice webhook** ingestion. A provider posts to `POST /api/contact-center/voice/webhook/{provider}`; the request is authenticated by the provider adapter's signature (not a user identity), so the endpoint is anonymous but every delivery must carry a valid signature. `IProviderVoiceWebhookAdapter` validates the signature and normalizes the payload into `ProviderVoiceEvent`s, `HmacProviderVoiceWebhookAdapterBase` provides constant-time HMAC-SHA256 verification, and `IProviderVoiceWebhookProcessor` resolves the adapter, verifies the signature, **requires an idempotency key on every parsed event**, and forwards accepted events to the provider voice event pipeline.
- See [Domain events and reliable dispatch](../contact-center/index.md#domain-events-and-reliable-dispatch).

#### Analytics: daily event metrics (Phase 12)

- Adds a rebuildable daily metrics projection. A `ContactCenterMetricsProjectionHandler` (running through the outbox on every domain event) records a per-day, per-event-type count in the `ContactCenterEventMetric` projection, and `IContactCenterMetricsService.GetSummaryAsync` returns export-ready totals by event type over a date range. Because the projection is derived purely from the durable event log, it can be rebuilt. The projection lives in the always-on base feature.

#### Recording and live monitoring (Phase 9)

- Adds recording orchestration. `IContactCenterRecordingService` tracks the interaction's `RecordingState` (none/recording/paused/stopped) through start, pause, resume, and stop, and publishes the matching `Recording*` domain events; provider modules execute the media capture.
- Adds supervisor live monitoring. `IContactCenterMonitoringService.EngageAsync` performs a scoped, audited monitor, whisper, barge, or take-over engagement, gated by the voice provider's capabilities, and publishes a `SupervisorMonitorStarted` event carrying the mode and supervisor. `ContactCenterVoiceProviderCapabilities` gains `Recording`, `Monitor`, `Whisper`, `Barge`, and `TakeOver` flags so provider modules advertise support and unsupported engagements are refused.

#### Optional Workflows bridge (Phase 11)

- When the **OrchardCore.Workflows** feature is enabled alongside Contact Center, a **Contact Center Event** workflow event activity becomes available. It starts or resumes a workflow when a Contact Center domain event is published, optionally filtered to a single event type, and exposes the event type, interaction id, aggregate, actor, and source component as workflow input. A feature-gated `ContactCenterWorkflowEventHandler` (running through the outbox) triggers the activity for every published domain event, so tenants can automate pre-call routing enrichment, post-call automation, and callback scheduling without custom code.

#### Data governance: interaction-event retention (Phase 13)

- Adds a data-retention purge. `IContactCenterRetentionService.PurgeInteractionEventsAsync` deletes durable interaction events older than a cutoff in bounded batches, and a daily `ContactCenterRetentionBackgroundTask` computes the cutoff from `ContactCenterRetentionOptions.InteractionEventRetentionDays` (bound from the `CrestApps_ContactCenter:Retention` configuration section; `0` keeps events indefinitely). This lets operators cap how long the durable event log is retained for privacy and storage governance.

#### AI assist seam (Phase 14)

- Adds a pluggable AI assist seam. `IContactCenterAssistProvider` lets AI modules contribute interaction summarization and disposition suggestions, and `IContactCenterAssistService` orchestrates the registered providers by order, returning the first result. Assistance is only available when a provider is installed (`IsAvailable`), so the Contact Center stays decoupled from any specific AI provider while supporting summaries, disposition suggestions, and future AI routing recommendations.

#### Agent desktop and supervisor dashboard (Phase 7)

- Adds the **Agent Workspace** (Contact Center Real-Time feature) under **Interaction Center → My workspace** — the full-screen desktop where an agent works the shift. It binds to the real-time hub and a live state endpoint to show presence with reason-code control, live queue depth, a ringing **offer card** with a response countdown, one-click **Accept** (which drives the accept-and-connect call command) and **Decline** (which re-offers the work), the **active interaction** panel with a live talk timer and a customer-360 link, an inline **disposition + notes** wrap-up, and recent history.
- Wrap-up completes through the source-neutral `IActivityDispositionService`, so the chosen disposition, any required-disposition rule, and the subject flow's follow-up actions run exactly as they do everywhere else in the CRM. The desktop uses change-detection rendering, so a live refresh never clobbers an agent's in-progress disposition or notes.
- Adds the **Supervisor Dashboard** under **Interaction Center → Live dashboard** (gated by `MonitorContactCenter`): live summary metrics, per-queue SLA-health tiles (waiting count, longest wait, and SLA breaches, colored as waits approach and then breach the threshold), and an agent presence board. It updates from the real-time hub with a periodic safety refresh so it can run on a wallboard.
- Adds supervisor live-engagement actions on active agent cards. **Monitor**, **Whisper**, **Barge**, and **Take over** call the audited monitoring service and are gated by the active voice provider's capabilities.
- Hardens agent offer and wrap-up ownership. Accepting or declining an offer now verifies that the reservation belongs to the current agent, completing work verifies that the activity belongs to the active or wrap-up interaction for that agent, and call end events move agents into wrap-up until completion releases them to their next presence state.
- Completes the agent accept-then-answer coordination: the Telephony soft phone now awaits the Contact Center accept before answering the agent's device, and only answers the device when the accepted offer reports `RequiresDeviceAnswer`. A call that has already been re-offered to another agent is never answered locally.
- See [Agent desktop & supervisor dashboard](../contact-center/agent-desktop.md).

#### Cleanup: a single voice-provider boundary

- Removes the superseded outbound `IDialerProvider` / `IDialerProviderResolver` provider abstraction and its `DialerDialRequest`, `DialerDialResult`, and `DialerProviderCapabilities` models. Outbound dialing already routes through `IContactCenterVoiceProvider`, so the Contact Center now exposes a single voice-provider boundary instead of two overlapping ones. Provider modules such as DialPad implement only `IContactCenterVoiceProvider`.

### Content Access Control

#### Role-based content restrictions

- `v2.0.0` includes the `CrestApps.OrchardCore.ContentAccessControl` feature as part of the broader module surface.
- The feature lets administrators restrict access to content items by attaching `RolePickerPart` to a content type and requiring one or more roles for access.
- Access control is enabled through `RolePickerPartContentAccessControlSettings`, including the `Restrict content?` setting that turns on enforcement for the attached content type.
- The release also adds recipe-schema coverage for `RolePickerPartContentAccessControlSettings`, which keeps content-definition and recipe-based provisioning aligned with the feature.
- Content Access Control admin editors now use the current Orchard admin field layout so labels, wrappers, and validation stay consistent with the rest of the v2 admin experience.
- See [Content Access Control](../modules/content-access-control.md).

### Content Fields

#### Phone Numbers

- `v2.0.0` adds `CrestApps.OrchardCore.ContentFields` with a `PhoneField` that stores phone numbers in E.164 format together with the ISO country code and national number.
- The editor uses `intl-tel-input` for country-aware entry and validates values through `IPhoneNumberService`.
- Omnichannel Management now migrates `PhoneNumberInfoPart.Number` from a `TextField` to a `PhoneField`.

### Content Transfer

#### Core transfer

- `v2.0.0` adds `CrestApps.OrchardCore.ContentTransfer`, `CrestApps.OrchardCore.ContentTransfer.Core`, and `CrestApps.OrchardCore.ContentTransfer.Abstractions`.
- CSV support stays in the base Content Transfer feature, while optional Excel workbook support moves into `CrestApps.OrchardCore.ContentTransfer.OpenXml`.
- Content-type settings now default **Allow Bulk Import** and **Allow Bulk Export** to enabled so types participate by default and can explicitly opt out.
- Import and export content-type selectors are now sorted alphabetically to make the bulk-transfer UI easier to scan.
- Imports now commit inline status changes before deferred background jobs run and use **Pending**, **Paused**, and **Deleting** states in the admin list.
- The admin actions now use **Pause import** and **Resume import** instead of the earlier cancel or process wording, and older canceled rows migrate to the paused state.
- Bulk imports now include **Publish imported content**, letting administrators choose between saving a latest draft and publishing immediately after create or update.
- Updates targeting an existing `ContentItemId` now apply imported values onto a new latest version and then save or publish that version according to the selected option.
- `ContentItemVersionId` is now treated as an export-only value for versionable content types and is ignored during import.
- The export pipeline now welds lazily created parts onto the parent content item during column discovery, import, and export so `.xlsx` exports no longer fail with a JSON node-cycle exception when a configured part is not yet materialized.
- Bulk import now supports very large files through a dedicated, content-transfer-specific chunked upload that is independent from the global media library limits. The new `MaxUploadFileSize` (default 1 GB), `MaxUploadChunkSize` (default 25 MB), and `TemporaryFileLifetime` (default 1 hour) settings under `OrchardCore:CrestApps:ContentTransfer` let administrators raise import limits without weakening media security, each upload request stays bounded to a single chunk, abandoned temporary files are purged automatically, and rejected uploads now return clear, specific error messages instead of a generic failure. The default chunk size stays below the common pre-application request-body limit (about 28.6 MB / `30000000` bytes enforced by IIS and many reverse proxies), so uploads work out of the box without extra host configuration. The settings can be overridden globally or per tenant; see the [Content Transfer configuration docs](../modules/content-transfer.md#overriding-the-upload-limits).
- The module now includes unit coverage for imported content-transfer services and Excel validation behavior, plus updated module documentation.

#### Omnichannel contact import and export integration

- Omnichannel contact imports and exports now map the first managed contact-method entries to workbook `Email`, `Cell Phone`, and `Phone` columns.
- The pipeline now reads and writes `DoNotCall`, `DoNotCallUtc`, `DoNotSms`, `DoNotSmsUtc`, `DoNotEmail`, `DoNotEmailUtc`, `DoNotChat`, and `DoNotChatUtc` on `OmnichannelContactPart`.
- The import metadata now advertises `true` and `false` for the `DoNot*` columns.
- Duplicate-phone filtering is enabled by default, skipped duplicate rows are added to the error export with a reason, and duplicate detection checks both the current import batch and existing Orchard contact records.
- Imports fall back to older stored phone values when upgraded tenants have not yet backfilled the newer normalized-phone indexes.
- When a row includes an existing `ContentItemId`, matching phone numbers on that same content item are treated as updates instead of duplicate conflicts.
- Omnichannel contact imports now require a single default country for non-E.164 phone numbers, normalize contact methods before duplicate and DNC checks, preserve existing E.164 values, and replace managed `Email`, `Cell`, and `Home` contact-method entries during updates so re-imports do not keep appending stale values.
- Omnichannel exports now include `TimeZoneId`, and imports can infer missing time-zone values from imported phone numbers through `IPhoneNumberService`.

### DNC Registry

#### Provider settings and abstractions

- DNC registry provider configuration is now split into dedicated settings pages under **Settings -> DNC Registries**, with separate **USA FTC Registry** and **Canada LNNTE-DNCL Registry** pages.
- Registry API keys are now stored as protected values instead of being shown back in the UI.
- DNC registry navigation now only shows provider pages when the matching feature is enabled, which removes broken links from the shared settings menu.
- DNC registry providers now expose their shared contract through `CrestApps.OrchardCore.DncRegistry.Abstractions`.
- `INationalDoNotCallRegistry` now supports an optional `NumberSearchContext` parameter with a `CountryCode` filter for country-specific lookups while remaining backward-compatible with existing implementations.

#### Local registry

- `v2.0.0` adds the **Local Do Not Call Registry** feature (`CrestApps.OrchardCore.DncRegistry.Local`), which lets administrators upload CSV files of phone numbers organized by country and manage those lists under **Interaction Center**.
- Local DNC uploads now persist the uploaded CSV first and finish processing in a background job after the request ends.
- Local DNC list management now exposes progress, processed row counts, success and error totals, and row-level validation details for blank rows, duplicate numbers, invalid numbers, header issues, and files with multiple populated columns.
- Local DNC list uploads and omnichannel phone endpoints now normalize numbers to E.164 through `IPhoneNumberService`, using the selected country as the parsing region and the E.164 number as the storage and comparison key.

### Omnichannel

#### Core platform

- `v2.0.0` introduces the Omnichannel platform together with management, Event Grid, and SMS integrations.
- `OmnichannelContactPart` now supports **Require time zone** and opt-in rendering of the Do not SMS, Do not chat, and Do not email preference fields, with **Require time zone** and **Use Do not call** enabled by default.
- Omnichannel contacts now store `TimeZoneId` directly on `OmnichannelContactPart`.
- Omnichannel contact definitions now enforce a fixed `ContactMethods` bag for content types that attach `OmnichannelContactPart`, and the named bag is injected from the content-definition build pipeline so it materializes even before stored-definition repairs run.
- Contact and communication-preference content-item indexes now subscribe to Orchard's default content-item collection instead of the older custom omnichannel document collection, so publish and update operations reliably refresh their index rows.
- Upgrade repairs now recreate the correct default-collection contact index tables, remove the empty legacy tables, and reindex published contacts so phone, time-zone, and do-not-call filters stay correct after upgrade.
- Omnichannel Management upgrade migrations now rerun both the default-contact-index repair and the legacy `PhoneNumberInfoPart.Number.Text` backfill for upgraded tenants, execute the phone rewrite through a direct deferred migration task, and reparse older North American lead numbers into `PhoneField` storage with a migration-only fallback.

#### Event Grid

- Azure Event Grid webhook authentication now supports metadata-backed bearer token validation through `AADMetadataAddress`, uses constant-time SAS-key comparison, and documents the exact webhook endpoint and required configuration.

#### Omnichannel Management

- Omnichannel Management moves disposition-driven behavior out of campaigns and into **Subject Flows** and **Subject Actions** under **Interaction Center -> Subject Flows**.
- **Breaking change:** The `OrchardCore.Workflows` dependency has been removed from the Omnichannel Management module. `CompletedActivityEvent`, `TryAgainActivityTask`, `NewActivityTask`, and `SetContactCommunicationPreferenceActivityTask` have been removed. Existing workflow-driven or campaign-action-driven setups must be migrated to subject flows and subject actions.
- Subject flows now separate **Configure** and **Manage Flow** responsibilities, add search-driven subject and action lists, highlight subjects with a **Missing flow** badge when no subject actions exist, and keep campaign editing focused on campaign metadata only.
- Automated subject flows now select a chat AI profile instead of configuring provider, connection, deployment, prompt, and tuning settings inline.
- Subject pickers only show subjects whose flows are fully configured, and activities and activity batches now resolve `CampaignId`, channel, interaction type, and channel endpoint from the selected subject flow.
- Omnichannel dispositions now use unique immutable names after creation, the delete action is wired correctly, activity batches no longer ask for a campaign, and user pickers now use the named user-search route instead of hard-coded admin API paths.
- Activity batches now use a source-selection modal before creation. Manual batches keep the existing selected-user assignment flow, while dialer batches hide the user selector and load activities as unassigned, available dialer work for later reservation.
- Activity batch sources are registered through `ActivityBatchSourceOptions`, allowing additional modules to add source-specific creation cards and display-driver-driven editor sections.
- Editing a completed omnichannel activity now suppresses workflow preview UI and does not imply that disposition changes will create new follow-up work.
- A new **Manage Activities** page (`Admin/omnichannel/manage-activities`) supports page-size selection, separate Contact and Activity filter groups, urgency icons, and six bulk actions against selected or filtered manual activities.
- Admin editors in Omnichannel Management now use the current `ocat-*` admin layout classes instead of older generic Bootstrap field markup.

#### SMS

- `v2.0.0` adds SMS channel support as part of the Omnichannel platform.
- The Twilio webhook now reuses the parsed form payload instead of reading the request body twice.

### Phone Number Verifications

#### Provider-agnostic verification framework

- `v2.0.0` introduces the **Phone Number Verifications** module (`CrestApps.OrchardCore.PhoneNumbers.Verifications`), a provider-agnostic framework for verifying contact phone numbers. The core feature is enabled on demand and does not depend on any external verification provider.
- The core feature now declares a dependency on `OrchardCore.Contents`, since it defines the `PhoneNumberVerificationPart` content part and a migration that requires `IContentDefinitionManager`. Enabling the feature on a tenant without content management previously failed to activate the shell.
- Shared phone-number services, constants, and verification index types now live in `CrestApps.OrchardCore.PhoneNumbers.Core`, so the Phone Numbers and Phone Number Verifications modules use the same core infrastructure.
- Verification providers are pluggable: they implement `IPhoneNumberVerificationProvider` from `CrestApps.OrchardCore.PhoneNumbers.Abstractions`, are registered against a unique provider key through `AddPhoneNumberVerificationProvider`, and configure localized display names and descriptions through the registration action. Providers are discovered dynamically so adding a provider never requires changes to the core feature.
- Provider HTTP payload parsing now uses shared snake-case JSON serializer options instead of per-property DTO attributes, keeping provider response models simpler and easier to maintain.
- The **Default provider** selector lists only providers that have been explicitly enabled (see the per-provider enable toggle below), is rendered before the revalidation interval, and the framework resolves providers by key through a lean `IPhoneNumberVerificationManager` without a provider ever selecting itself. When the selected provider is disabled or unset, the first enabled provider is used.
- Each provider settings tab now includes an **Enable this provider** switch, mirroring the Orchard Core SMS settings UX. A provider is only available for verification and only listed in the **Default provider** selector when its switch is on; the switch reveals the connection/authentication fields and gates their validation. Disabling the provider that is currently the default clears the default selection.
- Providers map their native responses into a shared `PhoneNumberVerificationResult` model that includes validity, reachability, line type, country, location, carrier, time zone, line status, risk details, the raw provider response, and an extensible metadata bag.
- Verification providers treat a supplied line status as part of the verification decision: numbers must be valid and have an `active` line status to be verified; providers that do not return line status continue to use the validity flag alone. AbstractAPI also maps `phone_validation.minimum_age` into the normalized result and queue display.
- Built-in provider HTTP clients are registered through `IHttpClientFactory` with the standard .NET HTTP resilience handler so AbstractAPI, Twilio, and Veriphone calls share consistent transient-failure handling.

#### Contact storage, indexing, and reporting

- Verification data is stored directly on content items through the new `PhoneNumberVerificationPart`, which retains the submitted and normalized phone number, full normalized provider response, last verification, verifying user id, attempt count, and next due date. User identifiers are stored instead of usernames because usernames can change over time.
- Omnichannel contacts now trigger phone-number verification automatically after create or update when their preferred phone number is new or changed. The handler first stores the changed number as `Unverified` during the current content save, then uses deferred work to call the provider only after the pending status is persisted. If no provider is currently available, the record remains pending for later revalidation.
- A `PhoneNumberVerificationPartIndex` SQL index exposes the commonly queried fields for reporting, dashboard widgets, revalidation jobs, and administrative searches, while provider-specific metadata stays in the stored JSON payload.
- A report dashboard under **Reports** -> **Phone Number Verifications** surfaces operational metrics such as verified, unverified, invalid, mobile, landline, and VoIP counts, numbers pending verification or requiring revalidation, the verification success rate, failures, and provider usage. Access to the dashboard is controlled by the dedicated `RunPhoneNumberVerificationsReport` permission.

#### Revalidation and cost optimization

- A throttled queue processor verifies pending records sequentially and applies the configured delay between provider calls, including across batches. The scheduled background task runs every five minutes to revalidate content items that already carry a stored phone number and whose verification has expired or remains pending, processing work in bounded resilient batches with distributed locking so long backlogs do not overlap provider calls. A new **Request delay (milliseconds)** setting (default `1000`) spaces out consecutive provider calls to respect provider rate limits (HTTP 429).
- Verification data on `PhoneNumberVerificationPart` acts as the authoritative cache: numbers are only (re)verified when expired or explicitly requested, which minimizes calls to paid provider APIs.

#### Resilient verification and records queue

- Verification now distinguishes a completed provider answer (`Verified`/`Invalid`) from a failed verification request (rate limits, HTTP errors, transport failures, or unparseable responses). A failed request never marks a number `Invalid`, and a previously verified number is never downgraded by a transient provider outage.
- `PhoneNumberVerificationPart` and its SQL index gained `FailedAttemptCount`, `LastError`, and `LastAttemptUtc`, so failed requests record the provider error and the consecutive-failure count on the content item. Existing tenants are upgraded by a data migration that adds the new columns and a retry index.
- A new **Maximum verification attempts** setting (default `3`) caps automatic retries. Failed records are retried by the background task until the cap is reached, after which they are flagged as **Needs attention** and excluded from the automatic retry query.
- A **Phone Verifications Queue** dashboard under **Tools** lets administrators monitor the verification queue: clickable status tiles show per-status counts and filter the list, a wide search box finds records by phone number, and records can be sorted by most/least recently attempted or newest/oldest created. The status tiles are now a mutually-exclusive partition that always sums to the total — **Pending** counts unverified records (including freshly re-queued ones), **Failed** counts records that can still be retried automatically, and **Needs attention** counts records that hit the attempt cap — fixing an earlier bug where the same record could be counted under multiple tiles. Each record shows the phone number, provider, result, attempt counts, last attempt, and any provider error. The list is paged and supports bulk actions: **Retry now** re-queues a single record, **Retry selected** (with a select-all checkbox) re-queues the records selected on the current page, and **Retry all failed** re-queues every failed or needs-attention record across all pages matching the current search. All retries are queued — they reset the failure counters, mark the records **Pending** immediately, and schedule deferred verification after the pending state is saved, which keeps the page responsive and avoids racing provider results against the request that queued them. The dashboard is gated by `RunPhoneNumberVerificationsReport`, and retries require `VerifyPhoneNumbers`.

#### AbstractAPI provider

- `v2.0.0` ships the **AbstractAPI Phone Number Verification** feature (`CrestApps.OrchardCore.PhoneNumbers.Verifications.AbstractApi`), which verifies numbers through the AbstractAPI Phone Validation service and maps the response into the common result model.
- AbstractAPI settings appear on a dedicated tab within the Phone Number Verifications settings page, following the Orchard Core SMS provider settings pattern, and the API key and other secrets are stored as protected values.
- AbstractAPI now sends only the `api_key` query-string authentication supported by the Phone Validation API. The settings UI no longer exposes Basic authentication, preventing stale proxy credentials from causing 401 responses when a valid API key is configured.
- AbstractAPI trims the API key before protecting it and after unprotecting it, preventing hidden copy/paste whitespace from being sent as part of the `api_key` query parameter.
- AbstractAPI now maps `phone_validation.line_status` and `phone_validation.minimum_age`, shows them in stored verification details and the Phone Verifications Queue, and uses the fixed Phone Intelligence endpoint in code rather than exposing an endpoint setting.
- Phone verification providers now write safe diagnostics for request start, endpoint path, credential-presence flags, HTTP status codes, response sizes, and normalized completion status without logging phone numbers or secrets.

#### Veriphone provider

- `v2.0.0` ships the **Veriphone Phone Number Verification** feature (`CrestApps.OrchardCore.PhoneNumbers.Verifications.Veriphone`), which verifies numbers through the Veriphone `/v2/verify` endpoint and maps the response into the common result model.
- Veriphone settings appear on a dedicated tab within the Phone Number Verifications settings page. The API key is stored as a protected value and sent to Veriphone with the recommended `Authorization: Bearer` header.

#### Twilio provider

- `v2.0.0` ships the **Twilio Phone Number Verification** feature (`CrestApps.OrchardCore.PhoneNumbers.Verifications.Twilio`), which verifies numbers through the Twilio Lookup v2 API and maps the response into the common result model.
- Twilio settings appear on a dedicated tab within the Phone Number Verifications settings page. The provider supports Twilio's API key SID/API key secret authentication and Account SID/Auth Token authentication, with secrets stored as protected values.
- The Twilio provider supports optional Lookup data packages through the `Fields` setting so administrators can request line type intelligence, line status, SMS pumping risk, and other paid add-ons when needed.
- Twilio and Veriphone now use fixed provider endpoints in code instead of exposing endpoint fields in site settings.
- Fixed the Twilio settings tab failing to render (a truncated response and `NS_ERROR_NET_PARTIAL_TRANSFER` in the browser): the localized endpoint hint containing a literal `{PhoneNumber}` token is now escaped so it is no longer treated as a composite format placeholder.

### Platform and tooling

#### Build, packaging, and shared runtime

- `v2.0.0` requires `.NET 10` and the Orchard Core `3.0.0-preview` package line used by this repository.
- Large parts of the reusable runtime moved out of Orchard-only projects and into shared `CrestApps.Core.*` packages. Custom modules that depended on older repo-local implementation assemblies should be rebuilt against the new package layout.
- Public async interfaces that can perform cancellable or longer-running work now accept an optional `CancellationToken cancellationToken = default` parameter. Custom implementations must update their signatures to match.
- The checked-in `NuGet.config` now restores Orchard Core packages from the Orchard Core preview feed and `CrestApps.Core*` packages from the CrestApps Core preview feed.
- Package tags are now computed centrally during packing so NuGet metadata is more consistent across the solution.

#### Frontend and workspace maintenance

- The root npm workspace now pins patched transitive versions of `lodash`, `yaml`, and `brace-expansion` through `overrides`, which removes the shared asset-build audit warnings.
- The repository `gulpfile.js` now waits for CSS and JavaScript output streams to finish before reporting task completion, which keeps `npm run rebuild` stable on current Node.js and Gulp versions.

### Recipes

#### Core recipe and schema platform

- `v2.0.0` expands the recipe surface, the surrounding samples, and the schema-export tooling.
- The recipe `content` step now enumerates `ContentType` values from tenant definitions, contributes content-type-specific part and field hints, exposes typed built-in field payloads, and keeps common metadata properties available for every item.
- Recipe content schemas now model built-in part payloads and additional Orchard settings more completely, including common body parts, title and autoroute payloads, scheduling settings, list settings, layout settings, HTML settings, and nullable built-in field payload values for standard Orchard field types.
- `ContentDefinition` and `ReplaceContentDefinition` schemas now register built-in Orchard Core field schema contributors such as `TextFieldSettings`, `MediaFieldSettings`, `TaxonomyFieldSettings`, `GeoPointFieldSettings`, and `MarkdownFieldSettings`.
- The Recipes module now also exposes the Orchard Core `move-attached-media-fields` step schema, validates `Roles.Permissions[]` entries against the permission enum correctly, omits placeholder permission values from generated role schemas, and treats `ContentDefinition` as valid when either `ContentTypes` or `ContentParts` is supplied.
- Recipe content-item schema composition now flows through `IContentItemSchemaService`, replacing the older `ContentCommonSchemas` helper.
- Recipe content schemas now understand `OmnichannelContactPart.TimeZoneId`, the E.164 `PhoneNumberInfoPart.Number.PhoneNumber` payload, `PhoneField` settings and payloads, and CrestApps schema contributors for parts such as `AIProfilePart`, `RolePickerPart`, `RolePickerPartContentAccessControlSettings`, and `UserFullNamePart`.
- The generic `Settings` schema now includes `LoginSettings.AllowRememberMe`, DNC registry site settings, and feature-gated contributed settings fragments for base settings such as `HomeRoute` as well as AI and user-profile settings.
- Deployment-plan steps now use localized admin categories, grouping AI, AI Data Sources, and MCP export steps under **Artificial Intelligence** and `TimeZoneMaps` under **Infrastructure**.
- The recipe schema exporter now instantiates service-backed schema steps such as `AdminMenu` instead of requiring parameterless constructors, and the `CrestApps.OrchardCore.RecipeSchemaExporter` utility now registers default `IOptions<AIOptions>` so AI-backed steps can be materialized successfully.

#### AI and contributor guidance

- AI recipe schemas now enumerate known nested metadata objects for `AIProfileTemplate`, `AIDataSource`, `McpConnection`, `McpPrompt`, `McpResource`, and `A2AConnection` while still allowing extension keys.
- The `AIProfile` recipe step now matches the current import and export contract, including the `Agent` profile type, prompt subject, orchestrator name, deployment selectors, and metadata fields while excluding obsolete selectors such as `ChatDeploymentId` and `UtilityDeploymentId`.
- The `AIProfile` and `CreateAIProfileFromTemplate` schemas now describe the known objects available under `Properties` and `Settings`, and `AIProfileTemplate` reuses that same known object catalog.
- Built-in CrestApps recipe-step schemas now provide richer per-property descriptions across steps such as Roles, Workflows, Media, Layers, Placements, Templates, Features, and related admin and import steps.
- The Recipes documentation now explains when to implement `PartSchemaDefinitionBase`, `FieldSchemaDefinitionBase`, and `IContentSchemaDefinition`, shows both part and field examples, and clarifies how multiple schema contributors for the same part or field name are merged.
- The Recipe Schema Exporter utility now lives under `utilities\` instead of `tests\`, and the documented build and run commands were updated accordingly.

### Telephony

#### Provider-agnostic soft phone

- `v2.0.0` adds the `CrestApps.OrchardCore.Telephony` module, which provides a provider-agnostic soft phone, a SignalR `TelephonyHub`, and multi-provider site settings modeled after the Orchard Core SMS provider pattern.
- The new `CrestApps.OrchardCore.Telephony.Abstractions` package defines the `ITelephonyProvider`, `ITelephonyService`, `ITelephonyProviderResolver`, `ITelephonyClient`, `ITelephonyAuthenticationProvider`, `ITelephonyAuthenticationService`, and `ITelephonyUserTokenStore` contracts together with the call models, provider options, settings, and permissions, so provider modules stay decoupled from the hub and UI.
- The hub routes soft phone requests (dial, hang up, hold/resume, mute, transfer, merge, send DTMF digits, answer, and reject) to the configured provider, runs each invocation in its own shell scope, and authorizes against the `Use the telephony soft phone` permission.
- The optional **Telephony Soft Phone** feature (`CrestApps.OrchardCore.Telephony.SoftPhone`) can display the floating soft phone on the admin dashboard, the front end, or both, and the `SoftPhoneWidget` shape can be placed manually in a theme or template.
- The soft phone is shown on the admin dashboard by default, and modules can contribute Display Management tabs/views to the widget.
- The soft phone supports multiple per-user authentication scenarios: providers that use shared, account-level credentials work without a per-user step, while providers that require OAuth 2.0 show a **Connect to provider** button that runs the authorization code flow and stores the user's tokens encrypted on their account. The widget authentication experience is extensible through `window.telephonySoftPhone.authHandlers`.
- The widget reports a live connection status and only enables the dial pad and call controls when the configured provider is available, connected, and authenticated. When no provider is enabled the header status reads **Not Ready** instead of a misleading **Ready** status, and during an active call the toggle button turns red and switches to a hang-up icon.
- The soft phone is draggable and can be moved anywhere on the screen, including the far right edge and on top of other widgets. Its position and open/closed state are persisted in `localStorage` and restored without a visible flash on the next page load, and by default it offsets itself to sit side by side with the AI chat widget when both are present.
- The widget footer is an extensible tab bar that switches between the **Keypad** view (dial pad and call controls) and the **Recent** view (call history). The hub exposes additional call controls (mute, unmute, and hold) and a call-history feed that surfaces active calls, recent inbound and outbound interactions, and missed calls highlighted in red with direction icons.
- Call interactions (interaction id, provider name, user, direction, outcome, timestamps, and duration) are persisted through `ITelephonyInteractionStore` and indexed with `TelephonyInteractionIndex`, so the site keeps a searchable call history independent of the provider for reporting and history tracking.
- The **Default telephony provider** selector lives on the **Soft Phone** settings tab, alongside the widget display options, so the default provider and the soft phone configuration are managed together.
- Phone Field editors and displays gain a click-to-dial button (through a neutral `data-phone-dial` placeholder) that places calls with the soft phone when it is present on the page.

#### Incoming-call modal and extensibility

- The soft phone now raises an incoming-call modal for a ringing inbound call with **Answer**, **Send to voicemail**, and **Ignore** actions. Voicemail is a new capability-gated provider operation (`ITelephonyProvider.SendToVoicemailAsync` with the `TelephonyCapabilities.Voicemail` flag); the DialPad provider implements it.
- Inbound calls are pushed to a specific user through the new `IIncomingCallDispatcher`, which runs every registered `IIncomingCallContextProvider`, builds an `IncomingCallContext`, and sends `IncomingCall(call, context)` through `IHubContext<TelephonyHub, ITelephonyClient>`.
- Other modules enrich the modal by contributing `IncomingCallCard` records (title, subtitle, badges, links, and an open URL). Each card renders an **Answer & open** shortcut, and a provider can supply `acceptUrl`/`declineUrl` properties so the modal reports the offer lifecycle. The Contact Center Voice feature uses this seam to list customers matched by the caller's number.
- See [Incoming calls](../telephony/index.md#incoming-calls).

#### DialPad provider

- `v2.0.0` adds the `CrestApps.OrchardCore.DialPad` module, which integrates the DialPad platform as a telephony provider through its REST API and contributes a **DialPad** tab to the telephony settings.
- DialPad supports an explicit **Authentication type** setting with **Select authentication type**, **API key**, and **OAuth 2.0** options. API key authentication uses the DialPad account that owns the key for simple integrations, while OAuth 2.0 lets each user connect their own DialPad account and is recommended for production multiuser integrations. The settings UI only shows the fields relevant to the selected mode, uses DialPad's fixed `https://dialpad.com/api/v2/` REST endpoint without an API base URL field, and links administrators to `developers.dialpad.com` for OAuth 2.0 access instructions. Secrets are stored encrypted through the data protection provider, and enabling or disabling DialPad updates the default telephony provider the same way the Orchard Core SMS Twilio provider manages the default SMS provider.
- The DialPad OAuth 2.0 integration is hardened to match DialPad's documented authorization code flow requirements: **PKCE** (RFC 7636) is always used (an `S256` challenge is sent during authorization and the verifier is supplied during the code exchange), the **`offline_access`** scope is always requested so refresh tokens are issued, and disconnecting a user calls DialPad's `deauthorize` endpoint to revoke the tokens issued on their behalf before the local tokens are removed.
- A new DialPad **Environment** setting selects between the **Production** (`dialpad.com`) and **Sandbox** (`sandbox.dialpad.com`) DialPad environments for both the REST API and the OAuth 2.0 authorize, token, and deauthorize endpoints, so an integration can be validated against the sandbox before going live.
- The telephony abstractions gained PKCE support (`TelephonyAuthorizationContext.CodeChallenge`/`CodeChallengeMethod`, `TelephonyCodeExchangeContext.CodeVerifier`, and `TelephonyAuthorizationRequest`) and token revocation (`ITelephonyAuthenticationProvider.SupportsProofKeyForCodeExchange` and `RevokeTokensAsync`), so any OAuth-based telephony provider can opt in to PKCE and provider-side token revocation on disconnect. OAuth completion now returns a `TelephonyResult`, so callers can inspect the failure reason instead of receiving only a boolean.
- The DialPad provider now resolves a named HTTP client configured by startup with standard ASP.NET Core HTTP resiliency policies for DialPad REST API and OAuth token requests.
- The **DialPad Contact Center Voice** feature now handles inbound calls end to end. A new `DialPadWebhookController` at `POST /api/dialpad/webhook/call` receives DialPad call-event webhooks, validated by a configurable **Webhook signing secret** (DialPad signs the payload as an HS256 JWT; validated without any external JWT dependency, with unsigned JSON accepted only when no secret is set). New inbound DialPad calls are normalized into an `InboundVoiceEvent` and routed through the Voice Contact Center Call Router — creating a CRM activity and voice interaction, resolving the entry point/queue, and offering the call to an available agent — while subsequent state events (ringing, connected, held, ended) are normalized into `ProviderVoiceEvent`s that update the interaction and call session. Together with the existing outbound dialing, DialPad can now serve as the complete phone provider for the Contact Center. The DialPad voice provider also advertises the `CallTransfer` capability.

### Time Zones

#### Time zone maps

- `v2.0.0` adds the `CrestApps.OrchardCore.TimeZones` module, which manages friendly named time-zone maps through admin UI, recipes, deployment plans, and a seed migration recipe.
- Omnichannel time-zone selectors now read from `ITimeZoneSelectListProvider` instead of Orchard Core's raw IANA list.
- The Time Zones admin list now shows the mapped `TimeZoneId` as a badge, renders the author through the shared `user-display-name` summary badge, and preserves `Author`, `OwnerId`, `CreatedUtc`, and `ModifiedUtc` metadata through recipe and deployment import and export.
- The Time Zones docs now clarify that the module overrides Orchard Core's `ITimeZoneSelectListProvider`, and the seed recipe now stamps admin-owned audit metadata through shared recipe variables.

## Migration from v1.x

### Step 1: Back up tenant data

Back up every tenant database before first startup on `v2.0.0`.

This is especially important for tenants that already contain:

- AI provider connections
- AI deployments
- AI profiles
- AI chat sessions

### Step 2: Update runtime, packages, and feeds

1. Install `.NET 10`.
2. Update to the `v2.0.0` package line.
3. Move projects to `net10.0`.
4. Adopt the v2 `NuGet.config` and preview package feeds.

### Step 3: Re-map enabled features

Review the v1 modules and provider-specific features you enabled and map them to the current v2 module set.

The major v2 module additions include:

- `CrestApps.OrchardCore.AI.A2A`
- `CrestApps.OrchardCore.AI.Chat.Claude`
- `CrestApps.OrchardCore.AI.Chat.Copilot`
- `CrestApps.OrchardCore.AI.Chat.Interactions`
- `CrestApps.OrchardCore.AI.DataSources`
- `CrestApps.OrchardCore.AI.DataSources.AzureAI`
- `CrestApps.OrchardCore.AI.DataSources.Elasticsearch`
- `CrestApps.OrchardCore.AI.Documents`
- `CrestApps.OrchardCore.AI.Documents.Azure`
- `CrestApps.OrchardCore.AI.Documents.AzureAI`
- `CrestApps.OrchardCore.AI.Documents.Elasticsearch`
- `CrestApps.OrchardCore.AI.Documents.OpenXml`
- `CrestApps.OrchardCore.AI.Documents.Pdf`
- `CrestApps.OrchardCore.AI.Mcp.Resources.Ftp`
- `CrestApps.OrchardCore.AI.Mcp.Resources.Sftp`
- `CrestApps.OrchardCore.AI.Memory`
- `CrestApps.OrchardCore.AI.Memory.AzureAI`
- `CrestApps.OrchardCore.AI.Memory.Elasticsearch`
- `CrestApps.OrchardCore.AI.Prompting`
- `CrestApps.OrchardCore.ContentFields`
- `CrestApps.OrchardCore.ContentTransfer`
- `CrestApps.OrchardCore.ContentTransfer.OpenXml`
- `CrestApps.OrchardCore.DncRegistry.Local`
- `CrestApps.OrchardCore.Omnichannel`
- `CrestApps.OrchardCore.Omnichannel.EventGrid`
- `CrestApps.OrchardCore.Omnichannel.Managements`
- `CrestApps.OrchardCore.Omnichannel.Sms`
- `CrestApps.OrchardCore.Recipes`
- `CrestApps.OrchardCore.TimeZones`

### Step 4: Migrate AI deployments and defaults

The v2 upgrade path must preserve legacy AI connections, deployments, profiles, and chat-session data from real `v1.x` tenant databases.

During deployment migration, verify:

- legacy `DictionaryDocument<AIDeployment>` records were imported
- missing v1 deployment fields such as `ModelName`, `ClientName`, and `Type` were normalized correctly
- default deployment site settings were backfilled
- site settings contain at least a default chat deployment when chat deployments exist

The default deployment backfill order should be:

1. UI-configured default deployment
2. legacy configured default from existing settings or appsettings-backed configuration
3. the first available deployment of that type

### Step 5: Review custom code

Review custom modules for:

- moved namespaces and package references
- legacy deployment-name usage
- older feature IDs
- provider-specific assumptions that no longer match the v2 module architecture
- interface implementations that now need `CancellationToken` parameters

### Step 6: Validate the upgraded tenant

After startup on `v2.0.0`, validate:

1. AI provider connections are present.
2. AI deployments are present and usable.
3. AI profiles load correctly.
4. AI chat sessions remain available.
5. Default chat deployment and related site defaults are set correctly.
6. Retrieval, document, MCP, Content Transfer, DNC, Omnichannel, Recipes, and Time Zone features still map to the intended modules and behaviors.

## Recommended reading

- [Artificial Intelligence Suite](../ai/index.md)
- [AI Chat](../ai/chat.md)
- [AI Chat Interactions](../ai/chat-interactions.md)
- [AI Chat Analytics](../ai/chat-analytics.md)
- [AI Prompt Templates](../ai/prompt-templates.md)
- [AI Data Sources](../ai/data-sources/index.md)
- [AI Documents](../ai/documents/index.md)
- [AI Memory](../ai/memory.md)
- [Model Context Protocol](../ai/mcp/index.md)
- [A2A](../ai/a2a/index.md)
- [Content Transfer](../modules/content-transfer.md)
- [Omnichannel Communications](../omnichannel/index.md)
- [Time Zones](../modules/time-zones.md)
