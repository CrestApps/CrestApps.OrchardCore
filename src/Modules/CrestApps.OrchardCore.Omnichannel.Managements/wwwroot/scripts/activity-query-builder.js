/*
** NOTE: This file is generated by Gulp and should not be edited directly!
** Any changes made directly to this file will be overwritten next time its asset group is processed by Gulp.
*/

function _createForOfIteratorHelper(r, e) { var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (!t) { if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e && r && "number" == typeof r.length) { t && (r = t); var _n = 0, F = function F() {}; return { s: F, n: function n() { return _n >= r.length ? { done: !0 } : { done: !1, value: r[_n++] }; }, e: function e(r) { throw r; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var o, a = !0, u = !1; return { s: function s() { t = t.call(r); }, n: function n() { var r = t.next(); return a = r.done, r; }, e: function e(r) { u = !0, o = r; }, f: function f() { try { a || null == t["return"] || t["return"](); } finally { if (u) throw o; } } }; }
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t["return"] && (u = t["return"](), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }
window.activityQueryBuilder = function () {
  'use strict';

  var defaultConfig = {
    containerId: 'query-builder',
    addFilterButtonId: 'add-filter-btn',
    filterFormId: 'filter-form',
    groups: {}
  };
  var config = {};
  var activeFilters = [];
  var filterCounter = 0;
  function initialize(options) {
    config = Object.assign({}, defaultConfig, options);
    activeFilters = [];
    filterCounter = 0;
    buildDropdownMenu();
    restoreActiveFilters();
  }
  function buildDropdownMenu() {
    var btn = document.getElementById(config.addFilterButtonId);
    if (!btn) {
      return;
    }
    var dropdownMenu = btn.nextElementSibling;
    if (!dropdownMenu) {
      return;
    }
    dropdownMenu.innerHTML = '';
    var _loop = function _loop() {
      var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
        groupKey = _Object$entries$_i[0],
        group = _Object$entries$_i[1];
      var header = document.createElement('li');
      header.innerHTML = '<h6 class="dropdown-header">' + escapeHtml(group.label) + '</h6>';
      dropdownMenu.appendChild(header);
      var _loop2 = function _loop2() {
        var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i2], 2),
          fieldKey = _Object$entries2$_i[0],
          field = _Object$entries2$_i[1];
        var item = document.createElement('li');
        var link = document.createElement('a');
        link.className = 'dropdown-item';
        link.href = 'javascript:void(0)';
        link.textContent = field.label;
        link.dataset.group = groupKey;
        link.dataset.field = fieldKey;
        link.addEventListener('click', function () {
          addFilter(groupKey, fieldKey);
        });
        item.appendChild(link);
        dropdownMenu.appendChild(item);
      };
      for (var _i2 = 0, _Object$entries2 = Object.entries(group.fields); _i2 < _Object$entries2.length; _i2++) {
        _loop2();
      }
    };
    for (var _i = 0, _Object$entries = Object.entries(config.groups); _i < _Object$entries.length; _i++) {
      _loop();
    }
  }
  function restoreActiveFilters() {
    var container = document.getElementById(config.containerId);
    if (!container) {
      return;
    }

    // Check for existing values in hidden inputs and show appropriate filters.
    for (var _i3 = 0, _Object$entries3 = Object.entries(config.groups); _i3 < _Object$entries3.length; _i3++) {
      var _Object$entries3$_i = _slicedToArray(_Object$entries3[_i3], 2),
        groupKey = _Object$entries3$_i[0],
        group = _Object$entries3$_i[1];
      var _loop3 = function _loop3() {
        var _Object$entries4$_i = _slicedToArray(_Object$entries4[_i4], 2),
          fieldKey = _Object$entries4$_i[0],
          field = _Object$entries4$_i[1];
        var existingInput = document.querySelector('[name="' + field.inputName + '"]');
        if (existingInput && existingInput.value) {
          addFilter(groupKey, fieldKey, existingInput.value);
        } else if (field.type === 'multiselect') {
          var existingInputs = document.querySelectorAll('[name="' + field.inputName + '"]');
          var values = [];
          existingInputs.forEach(function (input) {
            if (input.value) {
              values.push(input.value);
            }
          });
          if (values.length > 0) {
            addFilter(groupKey, fieldKey, values);
          }
        }
      };
      for (var _i4 = 0, _Object$entries4 = Object.entries(group.fields); _i4 < _Object$entries4.length; _i4++) {
        _loop3();
      }
    }
  }
  function addFilter(groupKey, fieldKey, presetValue) {
    var group = config.groups[groupKey];
    if (!group) {
      return;
    }
    var field = group.fields[fieldKey];
    if (!field) {
      return;
    }
    var container = document.getElementById(config.containerId);
    if (!container) {
      return;
    }
    var filterId = 'filter-' + filterCounter++;
    var filterRow = document.createElement('div');
    filterRow.className = 'query-builder-rule d-flex align-items-center gap-2 mb-2 p-2 border rounded bg-light';
    filterRow.id = filterId;
    filterRow.dataset.group = groupKey;
    filterRow.dataset.field = fieldKey;
    var badge = document.createElement('span');
    badge.className = 'badge text-bg-secondary';
    badge.textContent = group.label;
    filterRow.appendChild(badge);
    var label = document.createElement('span');
    label.className = 'fw-medium';
    label.textContent = field.label;
    filterRow.appendChild(label);
    var inputContainer = document.createElement('div');
    inputContainer.className = 'flex-grow-1';
    var input = createFieldInput(field, presetValue);
    inputContainer.appendChild(input);
    filterRow.appendChild(inputContainer);
    var removeBtn = document.createElement('button');
    removeBtn.type = 'button';
    removeBtn.className = 'btn btn-sm btn-outline-danger';
    removeBtn.innerHTML = '<i class="fa-solid fa-times"></i>';
    removeBtn.title = 'Remove filter';
    removeBtn.addEventListener('click', function () {
      removeFilter(filterId, field);
    });
    filterRow.appendChild(removeBtn);
    container.appendChild(filterRow);
    activeFilters.push({
      id: filterId,
      group: groupKey,
      field: fieldKey
    });
  }
  function createFieldInput(field, presetValue) {
    switch (field.type) {
      case 'select':
        return createSelectInput(field, presetValue);
      case 'multiselect':
        return createMultiSelectInput(field, presetValue);
      case 'datetime':
        return createDateTimeInput(field, presetValue);
      case 'text':
        return createTextInput(field, presetValue);
      default:
        return createTextInput(field, presetValue);
    }
  }
  function createSelectInput(field, presetValue) {
    var select = document.createElement('select');
    select.className = 'form-select form-select-sm';
    select.name = field.inputName;
    if (field.options) {
      var _iterator = _createForOfIteratorHelper(field.options),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var opt = _step.value;
          var option = document.createElement('option');
          option.value = opt.value;
          option.textContent = opt.label;
          if (presetValue && opt.value === presetValue) {
            option.selected = true;
          }
          select.appendChild(option);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }
    return select;
  }
  function createMultiSelectInput(field, presetValues) {
    var wrapper = document.createElement('div');
    wrapper.className = 'd-flex flex-wrap gap-1';
    if (field.options) {
      var _iterator2 = _createForOfIteratorHelper(field.options),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var opt = _step2.value;
          var checkDiv = document.createElement('div');
          checkDiv.className = 'form-check form-check-inline';
          var checkbox = document.createElement('input');
          checkbox.type = 'checkbox';
          checkbox.className = 'form-check-input';
          checkbox.name = field.inputName;
          checkbox.value = opt.value;
          checkbox.id = field.inputName.replace(/[\[\]\.]/g, '_') + '_' + opt.value;
          if (presetValues && Array.isArray(presetValues) && presetValues.includes(opt.value)) {
            checkbox.checked = true;
          }
          var label = document.createElement('label');
          label.className = 'form-check-label';
          label.htmlFor = checkbox.id;
          label.textContent = opt.label;
          checkDiv.appendChild(checkbox);
          checkDiv.appendChild(label);
          wrapper.appendChild(checkDiv);
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    }
    return wrapper;
  }
  function createDateTimeInput(field, presetValue) {
    var input = document.createElement('input');
    input.type = 'text';
    input.className = 'form-control form-control-sm';
    input.name = field.inputName;
    input.placeholder = field.placeholder || 'Select date...';
    if (presetValue) {
      input.value = presetValue;
    }

    // Initialize flatpickr if available.
    setTimeout(function () {
      if (typeof flatpickr !== 'undefined') {
        flatpickr(input, {
          dateFormat: field.dateFormat || 'Y-m-d',
          allowInput: true
        });
      }
    }, 0);
    return input;
  }
  function createTextInput(field, presetValue) {
    var input = document.createElement('input');
    input.type = 'text';
    input.className = 'form-control form-control-sm';
    input.name = field.inputName;
    input.placeholder = field.placeholder || '';
    if (presetValue) {
      input.value = presetValue;
    }
    return input;
  }
  function removeFilter(filterId, field) {
    var filterRow = document.getElementById(filterId);
    if (filterRow) {
      filterRow.remove();
    }
    activeFilters = activeFilters.filter(function (f) {
      return f.id !== filterId;
    });

    // Clear hidden inputs for this field.
    var inputs = document.querySelectorAll('[name="' + field.inputName + '"]');
    inputs.forEach(function (input) {
      if (input.closest('#' + config.containerId) === null) {
        input.value = '';
      }
    });
  }
  function escapeHtml(text) {
    var div = document.createElement('div');
    div.appendChild(document.createTextNode(text));
    return div.innerHTML;
  }
  return {
    initialize: initialize
  };
}();