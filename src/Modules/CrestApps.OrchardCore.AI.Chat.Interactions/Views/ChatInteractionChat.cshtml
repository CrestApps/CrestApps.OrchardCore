@using System.Text.Json
@using CrestApps.Core.AI.Capabilities
@using CrestApps.Core.AI.Chat.Realtime
@using CrestApps.Core.AI.Deployments
@using CrestApps.Core.AI.Models
@using CrestApps.OrchardCore.AI.Chat.Interactions.Hubs
@using CrestApps.OrchardCore.AI.Chat.Interactions.Settings
@using CrestApps.OrchardCore.AI.Chat.Interactions.ViewModels
@using OrchardCore.DisplayManagement.ModelBinding
@using OrchardCore.Settings

@model ChatInteractionCapsuleViewModel

@inject IUpdateModelAccessor UpdateModelAccessor
@inject ISiteService SiteService
@inject IAIDeploymentManager AIDeploymentManager
@inject IAIDeploymentCapabilityService CapabilityService
@{
    var baseId = Html.IdForModel();
    var chatAppHtmlId = $"{baseId}_ChatApp";
    var buttonHtmlId = $"{baseId}_SendButton";
    var userPromptHtmlId = $"{baseId}_UserPrompt";
    var placeholderHtmlId = $"{baseId}_SessionPlaceHolder";
    var chatContainerHtmlId = $"{baseId}_ChatContainer";
    var micBtnHtmlId = $"{baseId}_MicBtn";

    var site = await SiteService.GetSiteSettingsAsync();
    var chatModeSettings = site.GetOrCreate<ChatInteractionChatModeSettings>();
    var chatMode = chatModeSettings.ChatMode;
    var speechToTextDeployment = await AIDeploymentManager.ResolveOrDefaultAsync(AIDeploymentPurpose.SpeechToText);
    var textToSpeechDeployment = await AIDeploymentManager.ResolveOrDefaultAsync(AIDeploymentPurpose.TextToSpeech);
    var visionDeployment = await AIDeploymentManager.ResolveOrDefaultAsync(AIDeploymentPurpose.Vision);

    // Realtime (speech-to-speech): the interaction runs a realtime session when its selected deployment
    // declares the realtime capability, or when the site chat mode is Realtime and a realtime deployment
    // is available. Realtime is audio-only and takes precedence over the STT/TTS conversation pipeline.
    var realtimeDeployments = await CapabilityService.GetDeploymentsWithFeatureAsync(AIDeploymentFeatureNames.Realtime);
    var realtimeCapableDeploymentNames = realtimeDeployments
        .Where(d => !string.IsNullOrWhiteSpace(d.Name))
        .Select(d => d.Name)
        .ToArray();
    var selectedDeploymentName = Model.Interaction.ChatDeploymentName;
    var selectedDeploymentIsRealtime = !string.IsNullOrWhiteSpace(selectedDeploymentName)
        && realtimeCapableDeploymentNames.Contains(selectedDeploymentName, StringComparer.OrdinalIgnoreCase);
    var hasRealtime = selectedDeploymentIsRealtime
        || await CapabilityService.ResolveDeploymentWithFeatureAsync(AIDeploymentFeatureNames.Realtime) is not null;

    var effectiveChatMode = selectedDeploymentIsRealtime
        ? ChatMode.Realtime
        : chatMode switch
        {
            ChatMode.Realtime when hasRealtime => ChatMode.Realtime,
            // Realtime configured but no realtime deployment: fall back to STT/TTS conversation when possible.
            ChatMode.Realtime when speechToTextDeployment != null && textToSpeechDeployment != null => ChatMode.Conversation,
            ChatMode.Realtime => ChatMode.TextInput,
            _ => chatMode,
        };

    var realtimeEnabled = effectiveChatMode == ChatMode.Realtime;
    var realtimeWebRtcEnabled = realtimeEnabled && RealtimeTransportSettings.IsWebRtcEnabled(Context.RequestServices);

    var speechToTextEnabled = !realtimeEnabled
        && (effectiveChatMode == ChatMode.AudioInput || effectiveChatMode == ChatMode.Conversation)
        && speechToTextDeployment != null;
    var textToSpeechEnabled = !realtimeEnabled
        && (effectiveChatMode == ChatMode.Conversation || chatModeSettings.EnableTextToSpeechPlayback)
        && textToSpeechDeployment != null;
    var visionEnabled = !realtimeEnabled && visionDeployment != null;
    var conversationModeEnabled = speechToTextEnabled && textToSpeechEnabled;
    var conversationBtnHtmlId = $"{baseId}_ConversationBtn";
    var realtimeBtnHtmlId = $"{baseId}_RealtimeBtn";
    var realtimeVoiceGroupHtmlId = $"{baseId}_RealtimeVoiceGroup";
    var realtimeVoiceSelectHtmlId = $"{baseId}_RealtimeVoice";
    var realtimeVoicesUrl = Url.Action("Voices", "Realtime", new { area = "CrestApps.OrchardCore.AI.Chat" });
}

<div class="d-flex flex-column h-100">
    <div class="flex-grow-1 overflow-y-auto rounded-2 border bg-theme p-3 mb-3" id="@chatContainerHtmlId">
        <div class="w-100 ai-chat-messages">
            <div id="@placeholderHtmlId" class="text-center p-3@(Model.Prompts.Count > 0 ? " d-none" : string.Empty)">
                <h1>
                    @T["What do you want to know?"]
                </h1>
            </div>

            <div id="@chatAppHtmlId"></div>
        </div>
    </div>

    <div class="flex-shrink-0">
        @* Realtime voice picker. Hidden until the client detects a realtime-capable deployment. Populated
           from the selected realtime model via the voices endpoint. *@
        <div class="mb-2 d-none" id="@realtimeVoiceGroupHtmlId">
            <label class="form-label small mb-1" for="@realtimeVoiceSelectHtmlId">@T["Voice"]</label>
            <select class="form-select form-select-sm" id="@realtimeVoiceSelectHtmlId">
                <option value="">@T["Default voice"]</option>
            </select>
        </div>
        <div class="ai-chat-prompt-container bg-body text-body rounded border w-100">
            <textarea class="form-control bg-transparent border-0 chat-user-prompt-input px-4" id="@userPromptHtmlId" data-interaction-id="@Model.Interaction.ItemId" placeholder="@T["Message AI Chat"] ..." aria-label="@T["Message AI Chat"]" aria-describedby="@buttonHtmlId"></textarea>

            @* Single flex row: the realtime "Start speaking" button (flex-grow-1) can span the full input
               width when the client hides the text controls (audio-only). The shared realtime-audio
               controller inserts its settings gear as a sibling of this button (beside it) and appends the
               status line + push-to-talk bar to this row's parent (the prompt container), below the row —
               mirroring the CrestApps.Core layout. *@
            <div class="d-flex gap-2 align-items-center px-3 my-2">
                @* Realtime toggle. Hidden until the client detects a realtime deployment; then it spans the row. *@
                <button class="btn btn-outline-dark flex-grow-1" type="button" id="@realtimeBtnHtmlId" title="@T["Start speaking"]"
                        data-start-title="@T["Start speaking"]" data-end-title="@T["End Conversation"]"
                        data-start-html="<i class='fa-solid fa-wave-square me-1'></i> @T["Start speaking"]"
                        data-end-html="<i class='fa-solid fa-stop me-1'></i> @T["End Conversation"]"
                        hidden>
                    <i class="fa-solid fa-wave-square me-1"></i>
                    <span>@T["Start speaking"]</span>
                </button>

                <div class="d-flex gap-2 align-items-center ms-auto">
                    <button class="btn btn-outline-secondary rounded stt-mic-btn" type="button" id="@micBtnHtmlId" title="@T["Voice input"]" style="display: none;">
                        <i class="fa-solid fa-microphone"></i>
                    </button>
                    @if (conversationModeEnabled)
                    {
                        <button class="btn btn-dark rounded" type="button" id="@conversationBtnHtmlId" title="@T["Start Conversation"]" data-start-title="@T["Start Conversation"]" data-end-title="@T["End Conversation"]" data-start-html="<i class='fa-solid fa-wave-square'></i>" data-end-html="<i class='fa-solid fa-stop me-1'></i> @T["End Conversation"]">
                            <i class="fa-solid fa-wave-square"></i>
                        </button>
                    }
                    <button class="btn btn-dark" type="button" id="@buttonHtmlId" disabled data-start-icon="<i class='fa-solid fa-arrow-up'></i>" data-stop-icon="<i class='fa-solid fa-stop'></i>">
                        <i class="fa-solid fa-arrow-up"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
@{
    var prompts = new List<object>();

    if (Model.Prompts != null)
    {
        foreach (var prompt in Model.Prompts)
        {
            prompts.Add(new
            {
                Id = prompt.ItemId,
                prompt.Role,
                Content = prompt.Text,
                prompt.Title,
                prompt.IsGeneratedPrompt,
                prompt.References,
            });
        }
    }

    var chatInteractionConfigJson = JsonSerializer.Serialize(new
    {
        signalRHubUrl = Html.SignalRHubUrl<ChatInteractionHub>(),
        appElementSelector = $"#{chatAppHtmlId}",
        chatContainerElementSelector = $"#{chatContainerHtmlId}",
        inputElementSelector = $"#{userPromptHtmlId}",
        sendButtonElementSelector = $"#{buttonHtmlId}",
        placeholderElementSelector = $"#{placeholderHtmlId}",
        messages = prompts,
        untitledText = T["Untitled"].Value,
        clearHistoryTitle = T["Clear History"].Value,
        clearHistoryMessage = T["Are you sure you want to clear the chat history? This action cannot be undone. Your documents, parameters, and tools will be preserved."].Value,
        clearHistoryOkText = T["Yes"].Value,
        clearHistoryCancelText = T["Cancel"].Value,
        chatMode = effectiveChatMode.ToString(),
        micButtonElementSelector = $"#{micBtnHtmlId}",
        conversationButtonElementSelector = $"#{conversationBtnHtmlId}",
        saveIndicatorElementSelector = "#settings-save-indicator",
        textToSpeechEnabled,
        visionEnabled,
        // Realtime (speech-to-speech). The client shows the realtime button and hides the text input when
        // the selected deployment is realtime-capable. Audio capture/playback, barge-in, echo control and
        // push-to-talk are handled by the shared realtime-audio.js controller (window.CoreAIRealtime).
        realtimeEnabled,
        realtimeWebRtcEnabled,
        realtimeCapableDeployments = realtimeCapableDeploymentNames.Select(name => name.ToLowerInvariant()).ToArray(),
        realtimeButtonElementSelector = $"#{realtimeBtnHtmlId}",
        realtimeVoiceName = Model.Interaction.RealtimeVoiceName,
        realtimeVoiceSelectElementSelector = $"#{realtimeVoiceSelectHtmlId}",
        realtimeVoiceGroupElementSelector = $"#{realtimeVoiceGroupHtmlId}",
        realtimeVoicesUrl,
    }, JOptions.CamelCase);
}

<style at="Head">
    .chat-user-prompt-input:focus {
        border: none;
        box-shadow: none;
    }
    .chat-parameters-content .nav-link {
        padding: 0.5rem !important;
    }
</style>

<style asp-name="AIChatApp" at="Head"></style>
<style asp-name="highlightjs" at="Head"></style>
@if (speechToTextEnabled)
{
    <style asp-name="SpeechToText" at="Head"></style>
}
@if (realtimeEnabled)
{
    @* The shared realtime-audio controller renders its settings popover with a few Bootstrap Icons classes
       (bi-gear, bi-mic). This host uses Font Awesome, so map those classes to the equivalent Font Awesome
       glyphs instead of loading Bootstrap Icons. *@
    <style at="Head">
        .bi-gear::before,
        .bi-mic::before,
        .bi-mic-fill::before {
            font-family: "Font Awesome 6 Free", "Font Awesome 5 Free", "FontAwesome";
            font-weight: 900;
            font-style: normal;
            display: inline-block;
        }
        .bi-gear::before { content: "\f013"; }
        .bi-mic::before, .bi-mic-fill::before { content: "\f130"; }
    </style>
}
<script asp-name="ChatInteractionApp" at="Foot"></script>
<div id="@($"{baseId}_Config")"
     class="d-none"
     data-chat-interaction-config="@chatInteractionConfigJson"></div>
