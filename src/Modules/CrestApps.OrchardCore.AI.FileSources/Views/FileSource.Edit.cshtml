@*
    The FileSource_Edit / FileSource_SummaryAdmin shapes. The display manager builds a shape named after the
    record type, so these are the wrappers every driver's own shapes are rendered into -- without them the
    editor fails with "The shape type 'FileSource_Edit' is not found".
*@
@await DisplayAsync(Model.Content)
