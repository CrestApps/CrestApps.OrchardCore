@*
    The WebCrawler_Edit / WebCrawler_SummaryAdmin shapes for the record type this feature shares with Web
    Crawlers. Both features display the same stored type, and either may be enabled without the other, so
    each ships its own copy of the wrapper rather than one depending on the other for a template.
*@
@await DisplayAsync(Model.Content)
