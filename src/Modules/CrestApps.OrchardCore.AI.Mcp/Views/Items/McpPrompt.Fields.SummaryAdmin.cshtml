@using CrestApps.OrchardCore.AI.Mcp.Core.Models

@model McpPrompt

<a asp-route-action="Edit" asp-route-controller="Prompts" asp-route-id="@Model.ItemId" class="text-body">
    <strong>@Model.DisplayText</strong>
</a>
@if (!string.IsNullOrEmpty(Model.Prompt?.Name))
{
    <span class="text-muted ms-2">(@Model.Prompt.Name)</span>
}
@if (!string.IsNullOrEmpty(Model.Prompt?.Description))
{
    <p class="text-muted mb-0 mt-1">@Model.Prompt.Description</p>
}
@if (Model.Prompt?.Arguments?.Count > 0)
{
    <div class="text-muted small mt-1">
        @T["{0} argument(s)", Model.Prompt.Arguments.Count]
    </div>
}
